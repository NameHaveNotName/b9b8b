# 工作日志 (Work Log)

格式：每次迭代结束后，在表格顶部追加新行（倒序）。

| 2026-06-13 | 生成软著申请材料 —— 源代码后 30 页 Word 文档 | `scripts/generate-copyright-doc.py`（扩展：新增 `FILE_ORDER_BACK` 与后 30 页生成逻辑；后 30 页启用单文件 37 行上限截断，保证工作流 9 步/视频合成/前端组件/管理后台全覆盖；页码起始设置为 31，页脚显示「第 X 页 共 60 页」；输出 `/mnt/agents/output/软著材料/源代码后30页.docx`） | ① **编排顺序**：工作流核心 API（assets/ideation/framework/style/character/concept/storyboard/keyframes/trailer/video-direct/video-segments/recharge/admin*）→ 视频与合成层（video-utils/video-segment-utils/bgm-generator/workflow-executor/workflow-state/workflow.ts/models-config）→ 前端工作流组件层（layout/dashboard/project page/workflow page/TopStepper/storyboard page/StoryboardTable/StoryboardCanvas/AppSidebar/Header/IdeaAnchor/StepBadge）→ 管理后台前端层（admin layout/analytics/users/recharges）；② **截断原则**：长文件按 37 行上限截断并标注 `/* ... 后续内容截断 ... */`，自然凑满 30 页（1500 行）；③ **页码连续**：后 30 页 `<w:pgNumType w:start="31"/>`，合并前 30 页后页码 1–60 连续；④ **文件覆盖**：后 30 页纳入 40 个文件，覆盖工作流 API、ffmpeg 合成、BGM 生成、前端 UI、管理后台；⑤ **输出验证**：后 30 页 docx 58KB，30 页，1500 行，Microsoft Word 2007+ 格式 | Word 打开后确认：① 后 30 页页码从 31 开始；② 每页约 50 行；③ 长文件有截断标记；④ 敏感信息已脱敏 |
| 2026-06-12 | 生成软著申请材料 —— 源代码前 30 页 Word 文档 | `scripts/generate-copyright-doc.py`（**新建**：按项目骨架层→基础设施层→业务 API 层→前端组件层的顺序读取源码；全局脱敏 API Key/Token/Secret/数据库连接字符串/邮箱/手机号/身份证号；A4 50 行/页、居中页眉页脚页码、文件分隔行、跨页续接标记；输出到 `/mnt/agents/output/软著材料/源代码前30页.docx`） | ① **编排顺序**：package.json → schema.prisma → next.config.mjs → tsconfig.json → lib/prisma.ts/auth.ts/auth-helpers.ts/points.ts/points-config.ts/operations.ts/queue.ts → app/api/projects/{route.ts,[id]/route.ts,assets/route.ts} → 创意扩散/框架/风格/人物/概念/分镜/尾帧/宣传片/直出视频/充值/管理后台 → 前端页面与组件；② **截断原则**：按顺序自然凑满 30 页（1500 行），在某文件 mid-page 截断并标注续接；③ **脱敏规则**：API Key、Bearer Token、Secret、数据库 URL、邮箱、手机号、身份证号、URL 凭证统一替换为占位符；④ **格式规则**：Consolas 8pt + 宋体兼容、固定 13.5pt 行距、每页 50 行、页眉「AI影视全流程工作流系统 V1.0」居中、页脚「第 X 页 共 Y 页」Word 域代码；⑤ **输出验证**：docx 文件 57KB，30 页，1500 行，Microsoft Word 2007+ 格式 | Word 打开后确认：① 共 30 页；② 每页约 50 行；③ 页眉/页脚/页码居中；④ 跨页文件有续接标记；⑤ 敏感信息已脱敏 |
| 2026-06-12 | 宣传片/直出视频模块分镜卡片式重构收尾（工作指令.txt 全量执行） | `lib/video-utils.ts`（`concatVideos` 删除 `-an`，拼接阶段保留音频流；新增 `hasAudioStream` 辅助函数；`mixAudioVideo` 改造：检测视频原声，有原声时以 0.3 音量与原声混音，无原声时直接以 BGM 作为音轨）, `lib/bgm-generator.ts`（**新建**：提取宣传片 BGM 生成逻辑，降级链 千问百聆 → MiniMax → 静音 AAC）, `lib/video-segment-utils.ts`（`composeVideo` 接入真实 BGM 生成；宣传片读取 framework 情绪/故事梗概生成 BGM，裁剪到总时长后混音；直出视频直接返回 concat 结果保留原声）, `prisma/migrations/20250612000000_add_video_segments/migration.sql`（**新建**：VideoSegment 表 + Project 扩展字段迁移脚本）, `工作指令.txt`（更新 Phase 2 SQL 与 schema.prisma 对齐；step_name 取值改为 'TRAILER' | 'VIDEO_DIRECT' 与代码一致） | ① **Phase 1 定位**：旧版 ffmpeg 逻辑在 `lib/video-utils.ts`，旧版视频生成在 `lib/api-clients/mock-video.ts`/`xiaomi.ts`，新版分片逻辑在 `lib/video-segment-utils.ts` 与 `app/api/projects/[id]/steps/{trailer,video-direct}/route.ts`；② **Phase 2 数据库**：schema.prisma 已含 VideoSegment 与 Project 扩展字段，补充迁移 SQL；③ **Phase 3-4 提示词预生成/单片段生成**：代码已实现 `generateSegmentPrompts` + `generateOneVideoSegment` + 卡片式前端；④ **Phase 5 ffmpeg 合成**：宣传片原声保留+BGM 0.3 音量混音，直出视频保留原声，BGM 走 AI 生成三级降级；⑤ **Phase 6 UI**：TrailerPanel/VideoDirectPanel 已实现常驻视频区+分镜卡片网格+双击跳转+时间轴高亮；⑥ **Phase 7 边界**：片段失败可单独重试、合成前校验 incomplete 片段、BGM 失败静默降级；⑦ **构建验证**：`npx next build` 通过，零新增编译错误 | 用户端到端验证：① 宣传片开始执行先显示分镜卡片网格；② 单卡片/批量生成进入生成状态；③ 合成后上方显示长视频、下方显示分镜卡片；④ 双击卡片视频跳转；⑤ 播放时当前卡片高亮 |
| 2026-05-30 | Round 6：修复人物设计/生图模块 5 个前端交互与提示词问题 | `lib/api-clients/index.ts`（`generateCharacterPortrait` 和 `generateStyleSamples` 解构并返回 `isMock`+`lastError`；prompt 注入单人肖像约束：`single person, solo, only one character, complete face clearly visible, front facing, full head in frame, centered composition`）, `app/api/projects/[id]/steps/character/route.ts`（generate-images 分支和默认兼容分支的 asset metadata 均保存 `aspectRatio`+`imageModel`+`isMock`+`mockReason`）, `app/api/projects/[id]/steps/character/regenerate/route.ts`（metadata 保存 `isMock`+`mockReason`；重新生成时从 `outputData.prompts` 读取最新 `englishPrompt` 注入 `character.description`）, `app/api/projects/[id]/steps/concept/route.ts`（generate-images 分支和默认兼容分支的 asset metadata 均保存 `aspectRatio`+`imageModel`）, `app/api/projects/[id]/steps/concept/regenerate/route.ts`（修复硬编码 `isMock: false` → `!!result.metadata?.isMock`）, `app/api/projects/[id]/steps/style/regenerate/route.ts`（metadata 保存 `isMock`+`mockReason`）, `components/generation/HoverImageBadge.tsx`（`isRegenerating` 时隐藏 Mock 角标，实现乐观更新）, `app/(dashboard)/project/[id]/workflow/page.tsx`（`CharacterPanel` 新增 `expandedPromptIds` state + `togglePromptExpand` + `handleUpdatePrompt` 500ms 防抖 PATCH 保存 + `savePrompts`；卡片底部新增可展开/可编辑的「英文提示词」区域，使用 `ClickToEdit` 组件）, `工作指令.txt`（5 个 Phase 全部标记完成） | ① **Phase 1 Mock 标记**：后端全链路传递 `isMock`（character/style/concept 初始生成+重新生成），前端 `isRegenerating` 时立即隐藏 Mock 角标；② **Phase 2 英文提示词缺失**：卡片底部新增可展开区域，从 `asset.metadata?.llmPrompt` 读取显示；③ **Phase 3 多人问题**：prompt 强制注入单人肖像约束，压制多人出现概率；④ **Phase 4 角标固定**：后端补上 `aspectRatio`+`imageModel` 字段缺失，前端已动态读取；⑤ **Phase 5 提示词编辑**：`ClickToEdit` 点击编辑+500ms 防抖 PATCH 保存到 `outputData.prompts`，重新生成时读取最新 `englishPrompt`；⑥ **构建验证**：`npx next build` 通过，零新增编译错误 | 用户端到端验证：① 重新生图时 Mock 角标立即消失；② 角色卡片显示并展开英文提示词；③ 编辑英文提示词后重新生成使用新提示词；④ 角标正确反映实际生成参数 |
| 2026-05-25 | 动态故事结构改造 — 5分档选择器+动态幕结构+遍历acts+动态宣传片 | `prompts/ideation.txt`（删除固定三幕/30镜头/两分钟硬编码，增加故事分档判断指令，新增 storyLength/storyLengthLabel/storyLengthDesc 输出字段）, `app/api/projects/[id]/steps/ideation/route.ts`（POST 解析并保存 storyLength 三字段；PATCH 扩展支持保存 storyLength/storyLengthLabel/storyLengthDesc；Asset metadata 同步保存分档信息）, `app/(dashboard)/project/[id]/workflow/page.tsx`（新增 STORY_LENGTH_OPTIONS 常量数组+StoryLengthSelector 组件：5 个 pill 按钮+悬停显示时长/幕数/镜头数/特征详情浮层；IdeationPanel 集成选择器，localStoryLength 状态+useEffect 同步+handleStoryLengthChange 300ms 防抖 PATCH 保存；FrameworkPanel 幕结构展示重构：每幕显示标题/预估时长/镜头数/节奏标签+content 和 keyScenes 可编辑；新增环境设定/整体节奏策略/分档与总时长编辑面板）, `app/api/projects/[id]/steps/framework/route.ts`（新增 buildFrameworkPrompt 函数替代 loadPromptTemplate('ideation')：注入 storyLength 档位上下文+删除固定幕数/镜头数假设+AI 自行判断动态结构；输出归一化：动态 acts 数组(1-5幕，每幕 actNo/title/content/estimatedDuration/estimatedShots/pacing/keyScenes)+environments+overallPacing+storyLength+totalDuration；PATCH 扩展支持新字段保存）, `prompts/concept.txt`（删除"每幕2张/六张画面"硬编码，改为"根据每幕核心场景数量自行判断"）, `prompts/storyboard.txt`（删除"30个镜头/三幕结构"硬编码，改为"自行判断总镜头数和分配"）, `prompts/storyboard-act-dynamic.txt`（**新建**：通用动态分镜幕模板，接收 ACT_NUMBER/ACT_TITLE/ACT_CONTENT/ESTIMATED_SHOTS/ACT_PACING/KEY_SCENES 变量，AI 按预估镜头数生成对应数量镜头）, `app/api/projects/[id]/steps/storyboard/route.ts`（generateStoryboardByAct 改为通用动态模板；generate-prompts 和默认流程均改为遍历 framework.acts 数组动态生成；actsSummary 从固定 act1/act2/act3 改为动态映射）, `app/api/projects/[id]/steps/concept/route.ts`（每幕概念图数量基于 keyScenes 长度动态计算：Math.max(1, Math.min(keyScenes.length, 3))，至少1张最多3张；generate-prompts 和默认流程均适配）, `lib/llm-parser.ts`（幕数匹配正则从限制[一二三]3幕改为支持[一二三四五]任意幕；循环上限从 Math.min(parts.length, 3) 改为 parts.length）, `lib/api-clients/mock-video.ts`（新增 STORY_LENGTH_TRAILER_CONFIG：sketch/short=12s/medium=25s/feature=50s/epic=55s；loadConceptsAndFramework 返回 storyLength；generateVideoPromptForConcept 接收 segDuration/segIndex/segCount 动态参数，删除"30秒/5秒"硬编码；generateTrailer 动态计算 trailerDurationSeg/segCount；BGM 千问百聆/MiniMax/silentBgm 全部使用动态 bgmDuration=trailerDurationSec）, `工作指令.txt`（7项子任务全部标记完成）, `.kimi/todo-list.md`（新增 Phase 7:动态故事结构改造，8条子任务全部完成） | ① **5档分档体系**：速写1-3min/短篇3-5min(默认)/中篇5-10min/长片10-20min/史诗20-30min，每档有推荐幕数和镜头数范围；② **动态 acts 结构**：framework 输出 acts 数组长度 1-5，每幕含 title/content/estimatedDuration/estimatedShots/pacing/keyScenes，AI 自行判断分配；③ **概念图动态数量**：不再固定每幕2张，基于 keyScenes 长度（1-3张/幕）；④ **分镜动态生成**：storyboard-act-dynamic.txt 通用模板遍历所有幕，按 estimatedShots 生成对应数量镜头；⑤ **宣传片动态时长**：sketch/short=12s/medium=25s/feature=50s/epic=55s，片段数=时长/单段时长向上取整；⑥ **旧数据兼容**：无 storyLength 默认 short，无 acts 空数组不报错，旧 acts 结构自动归一化；⑦ **TS编译**：零新增错误 | 用户端到端验证：① 新建项目→创意扩散→确认默认选中"短篇·3-5分钟"→可切换其他档位→保存成功；② 选择方向→框架搭建→确认 AI 根据档位生成对应幕数（短篇2-3幕）→每幕 estimatedShots 不固定；③ 概念图按实际幕数生成，每幕1-3张；④ 分镜设计按每幕 estimatedShots 初始化；⑤ 宣传片时长随档位变化 |
| 2026-05-27 | Docker 部署 + 局域网访问 + 全局路径修正（工作指令.txt 全量执行） | `Dockerfile`（新建：Node.js 18 Alpine + ffmpeg + Prisma generate + npm run build）, `.dockerignore`（新建：排除 node_modules/.next/.git/大文件/临时文件）, `docker-compose.yml`（新建：生产模式，端口3000，环境变量注入，卷映射 public/mock-storage 和 .temp）, `docker-compose.dev.yml`（新建：开发模式，源码热挂载，node_modules 缓存隔离）, `Makefile`（新建：build/run/dev/stop/logs 五个快捷命令）, `scripts/show-ip.js`（新建：遍历 networkInterfaces 打印局域网 IPv4 访问地址）, `package.json`（新增 dev:lan 脚本）, `lib/video-utils.ts`（makeTempDir 从 os.tmpdir() 改为项目 .temp 目录；resolveFfmpegPath 新增 FFMPEG_PATH 环境变量最高优先级）, `lib/api-clients/mock-video.ts`（generateDirectVideo 中 os.tmpdir() 改为 makeTempDir('direct-') 统一管理）, `.env.local`（新增 TEMP_DIR=.temp 和 FFMPEG_PATH=ffmpeg）, `.gitignore`（新增 .temp/ 排除）, `工作指令.txt`（新增执行状态标记表）, `.kimi/todo-list.md`（Phase 8 三条完成） | ① **临时目录策略**：从系统 Temp（os.tmpdir()）改为项目内 .temp，Docker 内无系统 Temp 且跨平台兼容；makeTempDir 自动创建 .temp 目录，removeDir 事后清理；② **ffmpeg 路径**：环境变量 FFMPEG_PATH 最高优先级，Docker 内覆盖为 'ffmpeg'（Alpine apk 安装），Windows 开发回退 ffmpeg-static 多层兜底；③ **Docker 分层缓存**：先 COPY package.json + lock → npm ci → 再 COPY prisma → npx prisma generate → 最后 COPY 全部源码，最大化层缓存；④ **DEMO 模式 Docker 兼容**：DATABASE_URL 为占位符时自动走 Mock Prisma，无需真实数据库即可启动；R2 凭据为占位符时自动切 public/mock-storage 本地兜底，无需真实云存储；⑤ **局域网暴露**：next dev -H 0.0.0.0 绑定所有网卡，show-ip.js 自动打印本机局域网 IP；⑥ **卷映射持久化**：docker-compose 中 public/mock-storage 和 .temp 映射到宿主机，容器重启数据不丢 | 验证 Docker 构建通过；同学另一台电脑访问 http://宿主机IP:3000 | 
| 日期 | 迭代内容 | 新增/修改文件 | 关键决策与问题 | 下一步计划 |
|:---|:---|:---|:---|:---|
| 2026-05-25 | 提示词卡片文本编辑交互优化 — 改为「点击触发+自适应高度」模式 | `components/ui/ClickToEdit.tsx`（**新建**:可复用组件，props为value/onSave/multiline/className/placeholder；状态:isEditing/editValue；进入编辑时textarea自动聚焦+光标移末尾+adjustHeight自适应高度；失焦时若值变化则调用onSave；ESC取消编辑恢复原值；静态文本态hover显示underline提示可编辑）, `app/(dashboard)/project/[id]/workflow/page.tsx`（导入ClickToEdit；PromptPreview的editable模式下：中文描述和英文prompt均替换为ClickToEdit组件，保留原有handleUpdatePrompt防抖500ms+savePrompts乐观更新逻辑；增加"中文描述"/"英文提示词"小标签区分字段；编辑框minHeight 40px/maxHeight 300px；英文prompt保持font-mono等宽字体；非editable模式保持原有静态文本/可折叠按钮不变） | ① **点击触发**:平时显示静态文本（带hover underline提示），点击后立即进入编辑态，不要延迟；② **自适应高度**:textarea高度根据scrollHeight自动调整，最小40px最大300px防止撑爆卡片；③ **保留原有保存逻辑**:ClickToEdit的onSave回调接入已有handleUpdatePrompt→防抖500ms→PATCH API，不改动后端；④ **ESC取消**:按ESC恢复原始值并退出编辑态；⑤ **自动聚焦**:进入编辑态时自动聚焦并将光标移到文本末尾；⑥ **5个步骤统一**:Style/Character/Concept/Storyboard/Keyframes均通过PromptPreview使用ClickToEdit，只需修改一处；⑦ **TS编译**:零新增错误 | 用户端到端验证:① 进入风格统一提示词预览→确认中文描述和英文prompt显示为静态文本，无编辑框；② 点击中文描述→立即变为textarea，高度刚好容纳现有文本；③ 增加多行文字→确认textarea高度自动增长；④ 点击卡片外部→失焦保存，恢复静态文本；⑤ 按ESC→取消编辑恢复原值；⑥ 人物设计/概念图/分镜设计/生成尾帧重复验证 |
| 2026-05-25 | 全局文本编辑与数据流贯通（7个步骤全部支持用户编辑）— 创意扩散/框架搭建/风格统一/人物设计/概念图/分镜设计/生成尾帧的AI生成文本全部可编辑 | `app/api/projects/[id]/steps/ideation/route.ts`（新增PATCH handler:验证directions数组→读取step→合并outputData→更新→[TEXT-EDIT-IDEATION]日志）, `app/api/projects/[id]/steps/framework/route.ts`（新增PATCH handler:接收background/styleGuide/characters/synopsis/acts/inspiration/visualStyle→合并existingOutput→$transaction双写workflowStep+project.framework→[TEXT-EDIT-FRAMEWORK]日志）, `app/api/projects/[id]/steps/character/route.ts`（新增PATCH handler:验证prompts数组→合并outputData→[TEXT-EDIT-CHARACTER]日志）, `app/api/projects/[id]/steps/concept/route.ts`（新增PATCH handler:验证prompts数组→合并outputData→[TEXT-EDIT-CONCEPT]日志）, `app/api/projects/[id]/steps/style/route.ts`（扩展PATCH:Array.isArray(prompts)分支优先处理文本编辑→保存prompts→[TEXT-EDIT-STYLE]日志;原有selectedStyleId/styleRefUrl分支保留）, `app/api/projects/[id]/steps/keyframes/route.ts`（扩展PATCH:同时支持body.keyframes和body.prompts数组保存→[TEXT-EDIT-KEYFRAMES]日志）, `app/(dashboard)/project/[id]/workflow/page.tsx`（**大量修改**:①导入useRef;②PromptPreview新增editable/projectId/stepType/onSaveSuccess props+localPrompts状态+handleUpdatePrompt防抖500ms+savePrompts乐观更新失败回滚;③PromptPreviewWithRatio透传编辑props;④IdeationPanel新增projectId/mutate props+localDirections状态+handleUpdateDirection防抖500ms+saveDirections;⑤FrameworkPanel新增projectId/mutate props+localOutput状态+分字段表单(textarea:灵感/背景/风格/角色描述/梗概/幕场景)+updateField防抖500ms+saveFramework;⑥Style/Character/Concept/Keyframes四个Panel的PromptPreviewWithRatio调用均传入editable+projectId+stepType+onSaveSuccess=mutate;⑦KeyframesPanel合并actionChangeMap从keyframes数据注入localShots+handleActionChange PATCH到keyframes端点）, `app/(dashboard)/project/[id]/workflow/_components/KeyframesTable.tsx`（Shot接口新增actionChange字段;SortableRow新增editingAction/actionValue状态+saveActionChange+可编辑textarea列;主组件新增onActionChange prop并传递给SortableRow;表头新增「动作变化(点击编辑)」列）, `app/(dashboard)/project/[id]/storyboard/_components/StoryboardTable.tsx`（Shot接口新增actionChange?:string字段）, `工作指令.txt`（14项子任务全部标记完成）, `.kimi/todo-list.md`（新增Phase 6.10:全局文本编辑与数据流贯通,14条子任务全部完成） | ① **后端7个PATCH统一模式**:auth检查→step查找→字段验证→merge existingOutput→prisma.workflowStep.update→[TEXT-EDIT-{STEP}]日志;framework特殊处理:同步更新project.framework保证后续步骤读取最新值;② **前端防抖统一500ms**:所有文本编辑使用useRef+setTimeout做500ms防抖,避免频繁PATCH;③ **乐观更新+失败回滚**:PromptPreview/IdeationPanel/FrameworkPanel均先更新本地状态再发PATCH,失败时setLocalState(lastSavedRef.current)回滚;④ **PromptPreview自包含编辑**:新增editable模式,中文描述和英文prompt均变为textarea,无需Panel额外逻辑;⑤ **FrameworkPanel分字段表单**:6个字段全部textarea,角色描述按卡片独立编辑,幕结构每场景独立编辑,符合「JSON编辑不暴露原始结构」约束;⑥ **KeyframesTable actionChange**:新增可编辑列,onBlur保存到KEYFRAMES outputData.keyframes[i].actionChange,与STORYBOARD shots分离存储;⑦ **数据流贯通**:所有编辑直接覆盖workflowStep.outputData,后续步骤读取时自然拿到最新值(数据库查询无缓存);⑧ **TS编译**:零新增错误(仅mock-video.ts历史6个this any) | 用户端到端验证:① 创意扩散完成后→修改方向描述→失焦→终端[TEXT-EDIT-IDEATION]→刷新保持;② 框架搭建完成后→修改背景设定→失焦→终端[TEXT-EDIT-FRAMEWORK]→刷新保持;③ 风格统一提示词预览→修改英文prompt→失焦→终端[TEXT-EDIT-STYLE]→刷新保持;④ 人物设计/概念图/尾帧提示词同理验证;⑤ 生成尾帧表格→修改动作变化描述→失焦→终端[TEXT-EDIT-KEYFRAMES]→刷新保持;⑥ 进入下一步骤→确认读取的是编辑后的最新内容 |
| 2026-05-24 | 风格统一三模型智能分配（4 Phases全部执行）— LLM根据风格特性智能分配模型，3张风格图由3个不同模型生成 | `lib/models-config.ts`（新增`STYLE_MODEL_POOL`常量:3模型固定映射(flux.1-kontext-pro no.1/gpt-image-2 no.2/doubao-seedream-4.5 no.3)+strengths描述+short简称）, `prompts/style-generation.txt`（模板末尾新增模型分配指令:输出格式要求/模型编号说明/分配规则/示例输出，要求LLM输出modelNo字段且1/2/3不重复）, `lib/prompts.ts`（新增`assignModelNoFallback`函数:校验modelNo存在性→缺少则按索引兜底1/2/3→重复则强制重排→非法值重置为1）, `app/api/projects/[id]/steps/style/route.ts`（generate-prompts分支:解析后调用assignModelNoFallback，prompts保留modelNo，styleOptions保留modelNo，[STYLE-MODEL-ASSIGN]日志;generate-images分支:styleOptions携带modelNo传入processStyleGeneration）, `lib/style-processor.ts`（StyleOption接口新增modelNo字段;processStyleGeneration遍历每张图，根据opt.modelNo查找STYLE_MODEL_POOL对应模型，单独调用generateImage;[STYLE-GEN]日志"风格X: name → 模型N (id)";Asset metadata写入modelNo/modelId/modelLabel/aspectRatio/type:'style_sample'）, `app/(dashboard)/project/[id]/workflow/page.tsx`（导入STYLE_MODEL_POOL;PromptPreview卡片:有modelNo时顶部显示"将由 {label} 生成"蓝标;StyleCard:优先从metadata.modelNo映射STYLE_POOL.short简称(Flex/GPT/即梦)，旧项目回退到imageModel;HoverImageBadge新增modelShortLabel prop支持自定义简称;修复StyleCard onRegenerate未传参bug→透传(ar,model)到handleRegenerate）, `components/generation/HoverImageBadge.tsx`（新增`modelShortLabel`可选prop，优先于MODEL_SHORT_NAME映射显示角标）, `app/api/projects/[id]/steps/style/regenerate/route.ts`（导入STYLE_MODEL_POOL;删除旧Asset前读取metadata.modelNo/modelId;resolvedModel优先级:用户传入>原modelId>原modelNo映射>默认primary;[STYLE-REGENERATE]日志记录模型来源链） | ① **LLM决策+代码执行**:提示词模板要求LLM根据风格特性(二次元/写实/国风/科幻等)分配1/2/3，代码兜底校验不重复;② **3模型固定映射**:STYLE_MODEL_POOL作为唯一数据源，LLM和代码共用同一套定义;③ **旧项目兼容**:无modelNo的metadata重做时默认使用1号模型(flux.1-kontext-pro);④ **单张隔离**:每张风格图单独调用API，错误隔离+Mock兜底在generateImage内部完成;⑤ **角标简称差异化**:Flux/GPT/即梦(POOL.short) vs Flux-K/GPT-2/即梦4.5(SHORT_NAME)，风格统一使用更简洁的3字母简称;⑥ **TS编译**:零新增错误(仅mock-video.ts历史6个this any) | 用户端到端验证:① 新建项目→框架搭建→风格统一→生成提示词→确认3张预览卡片分别显示"将由Flux Kontext/GPT Image 2/即梦4.5生成";② 点击确认执行→终端[STYLE-GEN]日志确认3张图分别调用3个不同模型;③ 生成完成后3张风格图角标分别显示Flux/GPT/即梦;④ 悬停重做→默认锁定原模型→可切换→点击重做→[REGENERATE-PARAMS]日志确认使用新模型 |
| 2026-05-24 | 图像模型可用性修复（P0-1/P0-2/P1/P2 全部执行）— 即梦模型名校正+可灵降级+Flux 429重试+Gemini降级 | `lib/api-clients/xiaomi.ts`（新增`PROVIDER_MODEL_MAP`映射表:doubao-seedream-4.5→doubao-seedream-5.0-lite;新增`applyProviderModelMap`函数;`callXiaomiImageOnce`中自动应用映射,错误日志也显示映射后的模型名;`_generateImageInner`内层循环新增429指数退避重试:最多3次,间隔1s/2s/4s,非429错误走原有BACKOFF_MS逻辑）, `lib/models-config.ts`（kling-omni-image新增`disabled:true`,注释说明异步协议待接入;gemini-3.1-flash-image新增`disabled:true`,注释说明503渠道未挂载）, `app/(dashboard)/project/[id]/workflow/page.tsx`（`IMAGE_MODEL_OPTIONS`新增`.filter(m => !m.disabled)`,过滤已禁用模型）, `components/generation/HoverImageBadge.tsx`（模型选择按钮新增`.filter(m => !m.disabled)`过滤）, `.kimi/todo-list.md`（新增Phase 6.8:图像模型可用性修复,4条子任务全部完成）, `工作指令.txt`（4项修复逐一标记完成状态） | ① **P0-1即梦名校**:供应商不支持doubao-seedream-4.5 ID,通过PROVIDER_MODEL_MAP映射到doubao-seedream-5.0-lite,后续新增模型只需更新映射表;② **P0-2可灵降级**:kling-omni-image标记disabled:true,前端不展示,代码完全保留,待视频阶段接入异步协议(POST→task_id→轮询)后恢复;③ **P1 Flux 429**:429限流是临时状态,增加指数退避重试(1s→2s→4s,最多3次),非429错误(500/参数错误等)直接break不走重试;④ **P2 Gemini降级**:503表示代理层未挂载Gemini渠道,标记disabled:true,等供应商确认支持后恢复;⑤ **前端过滤**:2处遍历IMAGE_MODELS.available的位置(workflow/page.tsx的IMAGE_MODEL_OPTIONS+HoverImageBadge)均增加disabled过滤;⑥ **TS编译**:零新增错误 | 用户端到端验证:① 即梦生图确认终端不再fallback到gpt-image-2;② 模型选择栏不再显示可灵/Gemini;③ Flux生图遇429自动重试;④ 工作指令.txt全部标记完成 |
| 2026-05-24 | 图片角标+悬停交互式重做（4 Phases全部执行）— 5个生图步骤图片卡片增加比例/模型角标,悬停显示交互面板可切换比例/模型并触发重做 | `lib/models-config.ts`（新增MODEL_SHORT_NAME映射:doubao→'即梦4.5',gpt-image-2→'GPT-2',flux→'Flux-K',kling→'可灵O',gemini→'Gemini-F',grok→'Grok-4',qwen→'千问Max'）, `lib/api-clients/index.ts`（generateStyleSamples签名新增aspectRatio+imageModel参数,generateImage调用使用动态值+MODEL-SELECT日志）, `components/generation/HoverImageBadge.tsx`（**新建**:共享悬停交互组件,含常态角标{比例\|模型简称}+悬停覆盖层(bg-black/60)+重做按钮+6种比例选择按钮组+7模型选择按钮组+e.stopPropagation防事件冒泡+独立selectedRatio/selectedModel状态）, `app/(dashboard)/project/[id]/workflow/page.tsx`（导入HoverImageBadge+MODEL_SHORT_NAME;StyleCard图片区域替换为HoverImageBadge,onRegenerate签名改为(aspectRatio,imageModel);CharacterPanel/ConceptPanel卡片同理替换;StoryboardPanel表格缩略图增加HoverImageBadge包装;KeyframesPanel使用KeyframesTable已含角标;5个Panel的handleRegenerate全部新增aspectRatio+imageModel参数并传递到POST body）, `app/api/projects/[id]/steps/style/regenerate/route.ts`（body解构新增aspectRatio+imageModel,[REGENERATE-PARAMS]日志,generateStyleSamples传入新比例/模型,metadata写入aspectRatio+imageModel）, `app/api/projects/[id]/steps/character/regenerate/route.ts`（同上,generateCharacterPortrait传入新比例/模型,metadata写入）, `app/api/projects/[id]/steps/concept/regenerate/route.ts`（同上,generateConceptScene传入新比例/模型,metadata写入aspectRatio+imageModel）, `app/api/projects/[id]/steps/storyboard/regenerate/route.ts`（同上,SVG尺寸按MODEL_SIZE_MAP[newModel][newRatio]动态计算,metadata写入aspectRatio+imageModel）, `.env.local`（新增XIAOMI_API_KEY:sk-OGTmWg2yol...用于测试）, `scripts/test-7-models.ts`（七模型测试脚本,已运行）, `test-7-models/summary.json`（测试汇总:4成功/3Mock/0失败） | ① **组件化方案**:创建HoverImageBadge共享组件,5个面板的卡片全部复用,避免重复代码;② **状态独立**:每张卡片独立selectedRatio/selectedModel,互不影响;③ **悬停超时**:150ms延迟关闭防误触,覆盖层内所有按钮e.stopPropagation防触发lightbox;④ **数据源**:从asset.metadata?.aspectRatio/metadata?.imageModel读取,无则默认16:9+IMAGE_MODELS.primary;⑤ **后端统一**:4个regenerate路由全部接收aspectRatio+imageModel,[REGENERATE-PARAMS]日志,metadata写入;⑥ **Storyboard特殊处理**:SVG草图尺寸按MODEL_SIZE_MAP动态计算,支持6种比例;⑦ **generateStyleSamples升级**:接口新增aspectRatio+imageModel参数,内部generateImage使用动态值;⑧ **TS编译**:零新增错误;⑨ **测试脚本**:7模型测试通过(doubao/gpt-image-2/grok-4.2/qwen成功,flux 429/kling 500/gemini 503走Mock) | 用户端到端验证:① 5个步骤确认图片左下角有角标;② 悬停确认覆盖层出现;③ 切换比例/模型→点击重做→终端[REGENERATE-PARAMS]日志;④ 新图片角标显示新比例/模型 |
| 2026-05-24 | 七模型接入 Phase 1-4 全部执行 — 7个生图模型(doubao-seedream-4.5/gpt-image-2/flux.1-kontext-pro/kling-omni-image/gemini-3.1-flash-image/grok-4.2-image/qwen-image-max) | `lib/models-config.ts`（IMAGE_MODELS重写:primary='doubao-seedream-4.5'+available数组含7模型(id/label/provider/tags/defaultAspectRatio/supportedModes);MODEL_SIZE_MAP按模型/比例映射像素尺寸;删除旧版重复IMAGE_MODELS声明;保留Object.defineProperty STYLE/CHARACTER/CONCEPT/KEYFRAME别名+(IMAGE_MODELS as any).styleUnification/characterDesign/conceptArt/keyframes/representative兼容对象）, `lib/api-clients/xiaomi.ts`（新增MODEL_SIZE_MAP导入;classifyModel更新识别7模型(doubao/dalle/generic);buildPayload新增resolveSize函数优先查MODEL_SIZE_MAP[modelId][aspectRatio];doubao-multi/doubao/generic的size字段改用resolvedSize;dalle保持mapToDalleSize;generateImage positional参数默认值改为IMAGE_MODELS.primary）, `lib/api-clients/index.ts`（generateStyleSamples改为新对象参数+IMAGE_MODELS.primary+aspectRatio:'16:9';generateCharacterPortrait/generateConceptScene/generateKeyframe默认模型统一IMAGE_MODELS.primary;概念图size改为'1024x576'）, `lib/style-processor.ts`（IMAGE_MODELS.STYLE→IMAGE_MODELS.primary,3处替换）, `app/(dashboard)/project\[id]/workflow/page.tsx`（IMAGE_MODEL_OPTIONS改为从IMAGE_MODELS.available动态推导;所有defaultModel回退改为IMAGE_MODELS.primary,5处替换）, `app/api/projects/[id]/steps/character/route.ts`（completeStep保存imageModel改为IMAGE_MODELS.primary）, `app/api/projects/[id]/steps/concept/route.ts`（completeStep保存imageModel改为IMAGE_MODELS.primary;size改为'1024x576',2处替换）, `app/api/projects/[id]/steps/concept/regenerate/route.ts`（originalSize回退改为'1024x576'）, `app/api/projects/[id]/steps/keyframes/route.ts`（completeStep保存imageModel改为IMAGE_MODELS.primary）, `app/api/projects/[id]/steps/storyboard/route.ts`（新增IMAGE_MODELS导入;imageModel回退改为IMAGE_MODELS.primary） | ① **核心架构**:IMAGE_MODELS.available作为唯一数据源,前端下拉从中推导;MODEL_SIZE_MAP负责7模型×6比例的像素映射;resolveSize函数在buildPayload中统一调用;② **classifyModel更新**:doubao-multi(4-0/4-1)→doubao(seedream)→dalle(gpt-image)→flux→generic(kling/gemini/grok/qwen);③ **向后兼容**:Object.defineProperty别名+legacy config对象保留,但所有消费端统一改为IMAGE_MODELS.primary;④ **删除旧声明**:移除第219-262行重复IMAGE_MODELS声明,避免TS2451重声明错误;⑤ **TS编译**:零新增错误(仅mock-video.ts历史6个this any);⑥ **前端方案**:IMAGE_MODEL_OPTIONS动态推导,label/provider/desc直接从模型配置读取 | 用户端到端验证:① 风格统一提示词预览确认模型选择栏显示7选项;② 切换GPT Image 2→确认执行→终端[MODEL-SELECT]日志;③ 供应商日志确认调用对应模型;④ 刷新确认模型选择恢复;⑤ 5个步骤全部验证 |
| 2026-05-24 | 提示词预览增加生图模型选择栏 — 4种模型(豆包·即梦3.0/4.5/4.0 + DALL·E3) | `app/(dashboard)/project/[id]/workflow/page.tsx`（新增IMAGE_MODEL_OPTIONS常量;PromptPreview新增imageModel/onImageModelChange props+模型选择栏UI(绿色圆点状态指示);PromptPreviewWithRatio新增defaultModel参数+内部selectedModel状态管理;StylePanel/CharacterPanel/ConceptPanel/StoryboardPanel/KeyframesPanel全部传递defaultModel=step.outputData?.imageModel||对应IMAGE_MODELS.primary,onConfirm传(ratio,model)含imageModel字段;新增IMAGE_MODELS导入）, `app/api/projects/[id]/steps/style/route.ts`（generate-images接收body.imageModel,[MODEL-SELECT]日志,传递给processStyleGeneration,保存到outputData）, `app/api/projects/[id]/steps/character/route.ts`（同上,imageModel传给generateCharacterPortrait,completeStep保存imageModel,新增IMAGE_MODELS导入）, `app/api/projects/[id]/steps/concept/route.ts`（同上,imageModel传给generateConceptScene,completeStep保存imageModel）, `app/api/projects/[id]/steps/storyboard/route.ts`（接收body.imageModel,[MODEL-SELECT]日志,保存到outputData.imageModel）, `app/api/projects/[id]/steps/keyframes/route.ts`（接收body.imageModel,imageModel传给generateKeyframe,completeStep保存imageModel,新增IMAGE_MODELS导入）, `lib/style-processor.ts`（processStyleGeneration新增imageModel参数,[MODEL-SELECT]日志,generateImage调用改为imageModel||IMAGE_MODELS.STYLE,保存到outputData.imageModel）, `lib/api-clients/index.ts`（ImageClient接口+实现:generateCharacterPortrait/generateConceptScene/generateKeyframe全部新增imageModel参数,generateImage调用使用imageModel||对应IMAGE_MODELS.primary,新增[MODEL-SELECT]日志） | ① **前端方案**:IMAGE_MODEL_OPTIONS从models-config.ts实际配置推导(豆包3.0/4.5/4.0 + DALL·E3),模型选择栏在比例选择栏下方,绿色圆点状态指示;② **状态管理**:PromptPreviewWithRatio内部useState管理selectedRatio+selectedModel双状态,onConfirm传(ratio,model);③ **Model选择栏位置**:PromptPreview组件顶部信息栏下方,比例选择栏下方,提示词卡片上方;④ **后端方案**:5个路由generate-images分支接收body.imageModel,[MODEL-SELECT]日志前缀,保存到outputData.imageModel;⑤ **ImageClient层**:generateCharacterPortrait/generateConceptScene/generateKeyframe签名新增imageModel参数,generateImage调用使用imageModel||对应IMAGE_MODELS.primary;⑥ **Style**:processStyleGeneration新增imageModel参数,generateImage调用imageModel||IMAGE_MODELS.STYLE;⑦ **Storyboard**:SVG草图不涉及真实模型,但保存用户选择供后续参考;⑧ **默认值**:所有步骤从outputData?.imageModel||对应IMAGE_MODELS.primary读取;⑨ **日志前缀**:[MODEL-SELECT];⑩ **TS编译**:零新增错误 | 用户验证:① 5个步骤分别点击"开始执行"→提示词预览顶部出现模型选择栏;② 切换为"豆包·即梦 4.0"→点击"确认执行"→终端出现`[MODEL-SELECT]`日志;③ 供应商日志中确认调用的是选择的模型;④ 刷新页面确认模型选择栏默认显示上次选择 |
| 2026-05-24 | 提示词预览增加画面比例选择栏 — 6种预设比例(16:9/9:16/1:1/4:3/3:4/21:9) | `app/(dashboard)/project/[id]/workflow/page.tsx`（新增ASPECT_RATIO_OPTIONS常量;PromptPreview新增aspectRatio/onAspectRatioChange props+比例选择栏UI;新增PromptPreviewWithRatio包装组件管理内部比例state;StylePanel/CharacterPanel/ConceptPanel/StoryboardPanel/KeyframesPanel全部改用PromptPreviewWithRatio;onConfirm传递aspectRatio参数）, `app/api/projects/[id]/steps/style/route.ts`（generate-images接收body.aspectRatio,传递给processStyleGeneration,保存到outputData.aspectRatio）, `app/api/projects/[id]/steps/character/route.ts`（同上,aspectRatio传给generateCharacterPortrait）, `app/api/projects/[id]/steps/concept/route.ts`（同上,aspectRatio传给generateConceptScene）, `app/api/projects/[id]/steps/storyboard/route.ts`（接收aspectRatio,SVG草图尺寸按比例映射,保存到outputData.aspectRatio）, `app/api/projects/[id]/steps/keyframes/route.ts`（接收aspectRatio,传给generateKeyframe,保存到metadata+outputData）, `lib/style-processor.ts`（processStyleGeneration新增aspectRatio参数,generateImage调用从硬编码'16:9'改为动态aspectRatio）, `lib/api-clients/index.ts`（generateCharacterPortrait新增aspectRatio参数;generateKeyframe新增aspectRatio参数;两处generateImage调用改为aspectRatio） | ① **前端方案**:PromptPreviewWithRatio包装组件,内部useState管理selectedRatio,默认读取outputData.aspectRatio,避免5个Panel重复写状态管理;② **UI位置**:比例选择栏在顶部信息栏下方、提示词卡片上方,选中项黑色高亮,右侧显示尺寸数值;③ **Storyboard特殊处理**:SVG草图尺寸按比例映射(如9:16=576x1024),非真实AI生图但也遵循比例;④ **概念图/尾帧/人物**:ImageClient层已有aspectRatio支持(generateConceptScene/generateKeyframe),直接传递;generateCharacterPortrait新增aspectRatio参数;⑤ **Style**:processStyleGeneration新增aspectRatio参数,generateImage调用改用动态值;⑥ **默认值**:所有步骤默认16:9,上次选择保存在outputData.aspectRatio下次自动恢复;⑦ **日志前缀**:[ASPECT-RATIO];⑧ **TS编译**:零新增错误 | 用户验证:① 5个步骤分别点击"开始执行"→提示词预览顶部出现比例选择栏;② 切换为9:16→点击"确认执行"→终端出现`[ASPECT-RATIO]`日志;③ 图像生成完成后确认竖版图片(高度>宽度);④ 刷新页面确认比例选择栏默认显示上次选择 |
| 2026-05-24 | 缓冲阶段 Bug 修复：PROMPT_READY 状态不显示预览界面 | `app/(dashboard)/project/[id]/workflow/page.tsx`（StylePanel:PROMPT_READY判断移至PENDING之前;CharacterPanel:PROMPT_READY判断移至PROCESSING之前;ConceptPanel:PROMPT_READY判断移至PENDING之前,原`!step.outputData?.prompts?.length`守卫改为纯PENDING判断;KeyframesPanel:PENDING fallback按钮改为`action:'generate-prompts'`;StylePanel新增`[PROMPT-BUGFIX]`诊断日志） | ① **根因**:StylePanel/ConceptPanel中`if(step.status==='PENDING')`判断在PROMPT_READY之前,当后端返回`status:'PENDING'+outputData.prompts`时,前端先进入PENDING分支显示"待开始"UI,return后永远到不了下面的PROMPT_READY分支;② **CharacterPanel**:虽然PROMPT_READY在PENDING之前,但在PROCESSING之后,如果`isExecuting`仍为true会提前return;③ **修复策略**:所有面板PROMPT_READY判断统一移至最前,确保`step.status==='PENDING'&&step.outputData?.prompts?.length>0`优先匹配;④ **KeyframesPanel**:fallback按钮无action时后端默认`generate-images`,但无prompts会报错,改为默认`generate-prompts`;⑤ **TS编译**:零新增错误 | 用户验证:① 风格统一步骤点击"生成三种风格"→前端出现`[PROMPT-BUGFIX]`日志→显示提示词预览卡片→点击"确认执行"→生图;② 人物设计/概念图/分镜设计/生成尾帧同样验证;③ 观察终端日志:`[STYLE-PROMPT]`→`[STYLE-IMAGE]`等 |
| 2026-05-24 | 全局生图缓冲阶段：5个生图步骤(Style/Character/Concept/Storyboard/Keyframes)统一增加「提示词预览→确认执行」缓冲阶段 | `app/api/projects/[id]/steps/style/route.ts`（POST增加action分支:generate-prompts生成提示词存outputData.prompts返回PROMPT_READY;generate-images读取prompts执行生图;无action向后兼容）, `app/api/projects/[id]/steps/character/route.ts`（同上改造,prompts含characterName/characterId）, `app/api/projects/[id]/steps/concept/route.ts`（同上改造,prompts含actNumber/sceneIndex）, `app/api/projects/[id]/steps/storyboard/route.ts`（generate-prompts生成分镜JSON;generate-images生成SVG草图）, `app/api/projects/[id]/steps/keyframes/route.ts`（同上改造,为每条shot生成尾帧提示词）, `app/(dashboard)/project/[id]/workflow/page.tsx`（新增PromptPreview可复用组件:中英文提示词卡片网格+操作栏;5个Panel全部增加PROMPT_READY状态处理;PENDING按钮改为先传action:generate-prompts;确认执行传action:generate-images;KeyframesPanel新增keyframesRes SWR读取prompts） | ① **统一模式**:所有5个步骤POST handler接收action参数,generate-prompts只生成提示词存outputData.prompts,status=PENDING;generate-images读取prompts执行生图;无action时向后兼容走原有完整流程;② **PromptPreview组件**:序号角标+中文描述(主标题)+英文prompt(可折叠)+底部操作栏(重新生成/跳过/确认执行);③ **前端状态推断**:PROMPT_READY通过`step.status==='PENDING' && step.outputData?.prompts?.length>0`检测,无需新增Prisma enum;④ **KeyframesPanel特殊处理**:新增keyframesRes SWR从`/steps/keyframes`读取prompts;⑤ **TS编译**:零新增错误;⑥ **向后兼容**:所有路由无action参数时走原有完整流程,旧项目不受影响 | 用户验证:① 5个步骤分别点击"开始执行"→显示提示词预览卡片→点击"确认执行"→进入生成中→完成后显示结果图;② 提示词预览阶段点击"重新生成提示词"→后端重新调用LLM→前端展示新提示词;③ 旧项目(COMPLETED状态)正常显示生成结果,不受影响 |
| 2026-05-24 | 图像卡片自适应宽高比：Style/Character/Concept 三个 Panel 的图片卡片移除固定比例（aspect-square/aspect-video），改为根据实际图片 naturalWidth/naturalHeight 动态计算 aspectRatio | `app/(dashboard)/project/[id]/workflow/page.tsx`（新增 AspectAwareImage 子组件;StyleCard 新增 aspectRatio state+onLoad计算;CharacterPanel 新增 ratios Record 状态;ConceptPanel 新增 ratios Record 状态+默认1.78(16:9)占位;所有图片容器改用 style={{aspectRatio}}+transition-all duration-300平滑过渡） | ① **StyleCard**:每卡片独立aspectRatio state,从1:1占位开始,onLoad后设为实际比例;② **CharacterPanel/ConceptPanel**:在map循环中使用Record<string,number>存储每张卡片的ratio;③ **默认值**:Character默认1:1方图,Concept默认1.78(16:9)横图,Style默认1:1;④ **平滑过渡**:transition-all duration-300让比例变化不突兀;⑤ **overlay保留**:选中标志/重做按钮等绝对定位元素仍正常工作;⑥ **TS编译**:零新增错误 | 用户验证:① 风格统一步骤确认风格样图卡片高度随比例变化(横图矮竖图高);② 人物设计确认角色图卡片自适应(通常竖图会更高);③ 概念图确认场景图卡片自适应(横图会自然压缩高度) |
| 2026-05-24 | 表格缩略图显示修复：为所有 `<img>` 标签添加 `onError` 处理，图片加载失败时显示「加载失败」占位而非 broken image icon | `app/(dashboard)/project/[id]/storyboard/_components/StoryboardTable.tsx`（SortableRow的referenceImageUrl和firstFrameUrl的img标签增加onError:隐藏img+插入占位div）, `app/(dashboard)/project/[id]/workflow/_components/KeyframesTable.tsx`（SortableRow的firstFrameUrl和lastFrameUrl的img标签增加onError处理）, `app/(dashboard)/project/[id]/storyboard/_components/StoryboardCanvas.tsx`（画布视图img标签增加onError处理） | ① **根因**:代码已有`<img>`标签渲染缩略图,但图片加载失败时浏览器显示broken image icon,用户误认为是URL文本;② **修复策略**:onError处理—隐藏img+插入「加载失败」占位div;③ **占位样式**:bg-stone-200+text-xs+text-stone-500,与原有无图片占位符(bg-stone-100+ImageIcon)区分;④ **TS编译**:零新增错误 | 用户验证:① 分镜编辑器表格视图确认缩略图显示实际图片;② 生成尾帧界面表格视图确认首帧/尾帧显示实际图片;③ 故意断网或无效URL确认显示「加载失败」占位而非broken image |
| 2026-05-24 | 分镜导出 Excel 支持 + 生成尾帧去除重复视图切换按钮 | `app/(dashboard)/project/[id]/storyboard/page.tsx`（handleExport重构为双格式'json'\|'excel';导入xlsx+FileJson/FileSpreadsheet;按钮组UI替换单一按钮;导出数据结构含中文表头:镜头序号/镜头描述/运镜方式/时长/角色/图片URL/尾帧URL;列宽设置;@ts-ignore处理xlsx类型）, `app/(dashboard)/project/[id]/workflow/page.tsx`（同步修改:handleExport双格式;按钮组;修复Download重复导入;shot.sequence→index+1,shot.character→shot.characters.join）, `app/(dashboard)/project/[id]/workflow/_components/KeyframesTable.tsx`（移除viewMode状态和「表格视图/卡片视图」切换按钮组;删除SortableCard组件~150行;固定为表格视图;保留拖拽排序和行内编辑）, `package.json`（新增xlsx依赖） | ① **Excel导出**:xlsx包生成.xlsx Blob,中文表头与界面一致,图片URL完整保留可点击;② **UI方案**:项目无DropdownMenu,采用两个并排按钮(JSON+Excel);③ **重复视图按钮根因**:KeyframesTable内部有viewMode切换,KeyframesPanel顶层也有一组,导致两组同时出现;删除KeyframesTable内部的,保留顶层控制;④ **TS类型修复**:shot.sequence不存在→index+1;shot.character不存在→shot.characters数组join;⑤ **TS编译**:零新增错误 | 用户验证:① 分镜编辑器导出Excel→打开.xlsx确认中文表头+数据行对应+URL可点击;② 生成尾帧界面只有一组表格/画布切换按钮;③ JSON导出功能不受影响 |
| 2026-05-22 | 生成尾帧 UI 重构：与分镜设计双界面数据互通+表格/卡片双视图+拖拽编辑+导出 | `app/(dashboard)/project/[id]/storyboard/_components/StoryboardTable.tsx`（Shot接口新增`lastFrameUrl`可选字段）, `app/(dashboard)/project/[id]/workflow/_components/KeyframesTable.tsx`（完全重写：使用Shot接口替代旧Keyframe;双图显示-首帧只读正常亮度+尾帧可生成带Play按钮;拖拽排序;行内编辑描述/运镜/时长;saveShots指向STORYBOARD PATCH路由;新增单条尾帧生成handleGenerateLastFrame调用`/keyframes/generate-last`）, `app/(dashboard)/project/[id]/workflow/page.tsx`（KeyframesPanel完全重写：从STORYBOARD API读取数据替代旧KEYFRAMES API;useEffect+hasSynced同步模式与分镜设计一致;顶部工具栏含模式标签+导出按钮+生成全部尾帧+视图切换;canvas视图复用StoryboardCanvas）, `app/api/projects/[id]/steps/keyframes/generate-last/route.ts`（新建API：从STORYBOARD读取指定shot;获取风格参考;生成尾帧提示词;调用generateKeyframe;创建Asset;将lastFrameUrl写回STORYBOARD.outputData.shots;同步更新KEYFRAMES.outputData.results）, `app/(dashboard)/project/[id]/storyboard/page.tsx`（导出imageUrl增加lastFrameUrl回退） | ① **单一数据源**:所有分镜数据(STORYBOARD shots)含lastFrameUrl统一保存在STORYBOARD步骤;KEYFRAMES步骤仅保留生成结果缓存(compatibility);② **PATCH路由统一**:KeyframesTable编辑后PATCH到`/api/projects/[id]/steps/storyboard`,确保分镜设计SWR自动刷新;③ **尾帧生成两条路径**:单条`/keyframes/generate-last`(UI按钮)和批量`POST /keyframes`(生成全部按钮);④ **双向互通**:两个界面都SWR获取`/api/projects/[id]/steps/storyboard`,使用相同hasSynced模式,任何一边修改另一边自动同步;⑤ **首帧只读但正常亮度**:无grayscale/opacity降级,正常显示;⑥ **TS编译**:零新增错误 | 用户验证:① 分镜设计修改描述→切换生成尾帧确认同步;② 生成尾帧修改运镜→切换分镜设计确认同步;③ 拖拽重排→另一边确认顺序同步;④ 导出JSON包含lastFrameUrl;⑤ 单条生成尾帧按钮正常 |
| 2026-05-22 | 分镜导出按钮点击无反应修复：SSR防护+事件拦截+错误捕获 | `app/(dashboard)/project/[id]/storyboard/page.tsx`（handler增加`typeof window==='undefined'` SSR防护;try/catch包裹Blob下载逻辑;onClick改为`e.stopPropagation()+e.preventDefault()`防止DndContext拦截;全流程诊断日志:渲染时/handler触发时/下载成功时/失败时） | ① **根因分析**:按钮有onClick绑定,handler也存在,但缺少SSR防护(浏览器API在服务端不可用)+可能被子元素DndContext事件拦截+handler内部抛错无捕获;② **修复策略**:三管齐下—SSR防护+事件隔离+错误兜底;③ **诊断日志**:4级日志点确保可精确定位问题在哪个环节;④ **TS编译**:零新增错误 | 用户验证:进入分镜编辑器→点击按钮→控制台出现`[STORYBOARD-EXPORT] handler触发`→浏览器下载JSON→`[STORYBOARD-EXPORT] 下载触发成功` |
| 2026-05-22 | 分镜导出按钮：统一JSON导出逻辑，支持双模式imageUrl自动选择 | `app/(dashboard)/project/[id]/storyboard/page.tsx`（替换`exportReferenceTable()`→`handleExportStoryboard()`;导出结构{projectName,exportType:'storyboard',mode,exportTime,totalShots,shots:[{sequence,description,cameraMove,duration,imageUrl}]};imageUrl根据shotMode自动选择reference→referenceImageUrl,keyframe→firstFrameUrl;按钮文案「📥解读并导出分镜」,outline次要按钮样式;显示条件localShots.length>0;无图片时disabled+tooltip'分镜画面尚未生成';文件名`{projectName}_分镜解读_{YYYY-MM-DD}.json`;日志前缀`[STORYBOARD-EXPORT]`） | ① **统一导出**:原有"导出实拍参考表"仅支持reference模式且格式为简化表格,现改为统一JSON导出,覆盖两种模式;② **imageUrl选择逻辑**:基于当前shotMode运行时决定,无需额外状态;③ **空状态处理**:shots存在但无图片时按钮显示但disabled,tooltip提示原因;④ **纯前端**:Blob下载,不调后端API;⑤ **TS编译**:零新增错误(仅mock-video.ts历史6个this any) | 用户验证:① 实拍参考模式生成代表画面后出现按钮→点击导出→打开JSON确认referenceImageUrl字段完整;② 视频生成模式生成起始帧后同样出现按钮→导出JSON确认firstFrameUrl字段 |
| 2026-05-22 | 工作流看板重构修正：9步过滤/分镜模式前置/跳过按钮/首帧正常显示/导出按钮 | `app/(dashboard)/project/[id]/workflow/_components/TopStepper.tsx`（移除hiddenTypes,纯过滤只渲染VISIBLE_STEP_TYPES的9步,连接线改用displaySteps.length）, `app/(dashboard)/project/[id]/workflow/page.tsx`（新增storyboardMode状态+skipStep函数;StoryboardPanel新增模式选择按钮;Style/Concept/Trailer/KeyframesPanel新增跳过按钮+confirm确认对话框;StepHeader保留原有行为;新增Film图标导入）, `app/api/projects/[id]/steps/skip/route.ts`（新建:定义SKIPPABLE_STEPS,upsert创建SKIPPED状态步骤,返回nextStepType自动跳转）, `app/(dashboard)/project/[id]/workflow/_components/KeyframesTable.tsx`（移除首帧grayscale/opacity-70降级样式,改为正常亮度,标签统一为「首帧」/「尾帧」黑底白字）, `app/(dashboard)/project/[id]/storyboard/page.tsx`（移除模式切换Tabs+确认对话框,requestModeChange/handleModeConfirm函数,改为只读模式标签「当前:实拍参考模式/视频生成模式」,保留导出实拍参考表功能）, `app/api/projects/[id]/steps/storyboard/route.ts`（POST保存mode字段到outputData.mode） | ① **Stepper根因**:之前虽然用VISIBLE_STEP_TYPES过滤标题,但hiddenTypes逻辑将隐藏步骤仍渲染出来;修正为纯`steps.filter(s => visibleSet.has(s.stepType))`;② **模式前置**:分镜模式从编辑器内部移至工作流看板StoryPanel的PENDING状态,用户先选模式再点生成,API携带mode参数,编辑器内只读显示;③ **跳过机制**:新建独立`/skip`路由,定义SKIPPABLE_STEPS=['STYLE','CONCEPT','TRAILER','KEYFRAMES','REVIEW'],跳过后状态SKIPPED,自动跳转下一步;④ **首帧修复**:移除灰度+降级样式,改用标签区分来源;⑤ **导出按钮**:保留在分镜编辑器顶部,仅实拍模式且有图片时显示;⑥ **TS编译**:零新增错误;⑦ **验证**:localhost:3000 200 OK,Ready in 997ms | 用户验证:① 工作流看板只显示9步;② 分镜设计PENDING时显示两模式按钮;③ 风格/概念/宣传片/尾帧步骤有跳过按钮;④ 生成尾帧首帧正常亮度;⑤ 分镜编辑器顶部只读模式标签+导出按钮 |
| 2026-05-22 | 仪表盘双 Bug 修复：Date 序列化 + Redis 连接阻断（含路由文件补充） | `lib/prisma.ts`（新增 `reviveDates()` 递归函数,处理 createdAt/updatedAt/completedAt/startedAt/emailVerified;`loadMockStore` 中 `reviveDates(records)` 替代直接赋值）, `app/(dashboard)/dashboard/page.tsx`（L160 `p.createdAt.toISOString()` → `new Date(p.createdAt).toISOString()` 防御式保护）, `lib/queue.ts`（新增 `isDemoMode` 导出;DEMO 模式下 `redisConnection` 返回 Mock 对象不创建 IORedis;`createQueue`/`createWorker` 返回空操作 Mock;真实 Redis 逻辑完全保留）, `app/api/projects/[id]/steps/video-direct/route.ts`（`new Queue(...)` → `createQueue('video-generation')`,删除直接 import redisConnection）, `app/api/projects/[id]/steps/trailer/route.ts`（同上: `new Queue(...)` → `createQueue('video-generation')`） | ① **Bug1 根因**: Mock Prisma 持久化到 db.json 时 `JSON.stringify` 将 Date 转为 ISO 字符串,`JSON.parse` 不会自动还原为 Date 对象,调用 `.toISOString()` 崩溃;② **Bug2 根因**: 路由文件直接在模块顶层 `new Queue(...)` 而非使用 `createQueue()`,BullMQ Queue 构造函数触发 Redis 连接尝试;③ **修复策略**: reviveDates 在数据恢复层解决根本问题+前端 new Date() 防御式兜底;queue.ts 模块级 `isDemoMode` 检测短路+路由统一使用 `createQueue()`;④ **TS 编译**: 零新增错误(剩余 6 个 mock-video.ts this 隐式 any 为历史问题);⑤ **验证**: `/dashboard` 200 OK,`/api/.../video-direct` 200 OK,`ECONNREFUSED` 计数=0 | 用户验证: 浏览器进入 /dashboard 及项目工作流页面,确认无 toISOString/Redis 报错 | | 工作流 7 阶段重组：12步→9步、分镜双模式、生成尾帧、直生视频双策略、风格注入 | `lib/workflow.ts`（新增 `VISIBLE_STEP_TYPES` 9步数组, KEYFRAMES label→"生成尾帧", VIDEO_DIRECT描述更新）, `app/(dashboard)/project/[id]/workflow/_components/TopStepper.tsx`（基于 VISIBLE_STEP_TYPES 过滤渲染, 隐藏步骤仅历史项目显示, KEYFRAMES→"生成尾帧"）, `app/(dashboard)/project/[id]/workflow/page.tsx`（KEYFRAMES→"生成尾帧", 步数12→9, VideoDirectPanel 增加策略提示+projectId, KeyframesPanel 按钮文字更新）, `app/(dashboard)/project/[id]/storyboard/page.tsx`（Phase 2: shotMode 状态+模式切换确认对话框+hasSynced同步模式, Phase 3: exportReferenceTable JSON导出+条件渲染）, `app/(dashboard)/project/[id]/storyboard/_components/StoryboardTable.tsx`（Shot接口扩展mode/referenceImageUrl/firstFrameUrl, 接收mode prop, 模式特定缩略图+角标"参考/首帧"）, `app/(dashboard)/project/[id]/storyboard/_components/StoryboardCanvas.tsx`（mode prop, 模式特定大图+模式标签）, `app/api/projects/[id]/steps/storyboard/route.ts`（PATCH 保留mode字段到outputData）, `app/api/projects/[id]/steps/keyframes/route.ts`（Phase 4: 只生成尾帧, 从STORYBOARD读取firstFrameUrl作为只读引用, GET注入首帧, 无firstFrameUrl时阻断提示）, `app/api/projects/[id]/steps/video-direct/route.ts`（Phase 5: first-last/first-only双策略检测, 每shot独立策略, 返回strategy字段）, `app/api/projects/[id]/steps/character/route.ts`（Phase 6: 显式检测STYLE步骤状态, 注入失败不阻断, 无风格时文生图, [CHARACTER]日志前缀）, `app/(dashboard)/project/[id]/workflow/_components/KeyframesTable.tsx`（首帧灰度+只读样式, 表头"首帧(只读)", 空状态文字更新）, `components/generation/AssetCard.tsx`（KEYFRAMES→"生成尾帧"）, `components/generation/QueueMonitor.tsx`（KEYFRAMES→"生成尾帧"）, `app/(dashboard)/project/[id]/page.tsx`（KEYFRAMES→"生成尾帧"）, `lib/api-clients/index.ts`（generateCharacterPortrait styleRefUrl改为可选） | ① **数据库零破坏**: 不删除任何Prisma enum/表/字段, 隐藏步骤(REPRESENTATIVE/AI_RENDER/CAMERA_MOVEMENT)代码保留但不暴露入口; ② **Shot数据结构扩展**: 新增mode/referenceImageUrl/firstFrameUrl三个optional字段, 完全兼容旧项目JSON; ③ **分镜双模式**: 实拍参考模式(产出代表画面)+视频生成模式(产出起始帧), 共用同一套表格/拖拽/保存基础设施, 切换时二次确认清空已生成画面; ④ **生成尾帧精简**: 首帧在尾帧步骤中绝对只读(来自分镜设计firstFrameUrl), 只调用尾帧生成, 首帧灰度显示; ⑤ **直生视频双策略**: 有尾帧走first-last, 无尾帧走first-only, 支持单shot独立策略(部分生成情况); ⑥ **风格注入容错**: getStyleRefUrl失败不阻断, 静默降级为文生图; ⑦ **TS编译**: 零新增错误(剩余6个mock-video.ts历史this错误与本轮无关) | 端到端验证: 新建项目走9步流程; 分镜双模式切换+导出JSON; 生成尾帧(基于起始帧); 直生视频两种策略; 跳过风格统一的人物设计 |
| 2026-05-19 | 单条重做+阶段整体重做功能（Phase 1-6 全部执行，5 个阶段通用化） | `app/(dashboard)/project/[id]/workflow/page.tsx`（`StepContent` 新增 `mutate`/`setToast` props;`ConceptPanel`/`CharacterPanel`/`StylePanel`+`StyleCard`/`StoryboardPanel`/`TrailerPanel` 全部添加悬停"重做"按钮+底部"重新生成全部"二次确认;`CharacterPanel`/`StoryboardPanel`/`TrailerPanel` 新增 `projectId`/`onError`/`mutate`/`setToast` props）, `app/api/projects/[id]/steps/{concept,character,style,storyboard}/regenerate/route.ts`（新建×4:单条重做API,通过 assetId/styleId/shotId 定位→删除旧Asset→重新生成→更新 outputData）, `app/api/projects/[id]/steps/{concept,character,style,storyboard,trailer}/route.ts`（POST 全部支持 `force=true` 参数:跳过缓存→删除旧Asset→重置 status=PENDING→重新生成） | ① **设计决策**:单条重做走 `POST .../regenerate` 独立路由,整体重走复用现有 POST + `force=true` 跳过缓存;② **数据覆盖策略**:单条替换(保持数组长度不变),整体清空后重新生成;③ **5个阶段覆盖**:CONCEPT(单条+整体)→CHARACTER(单条+整体)→STYLE(单条+整体)→STORYBOARD(单条+整体)→TRAILER(整体,单条因管线复杂暂跳过);④ **UI统一**:每张卡片/每行悬停显示↻重做按钮,底部"重新生成全部"带二次确认对话框,生成中禁用按钮+显示loading spinner;⑤ **TS编译**:零新增错误(剩余8个历史错误与本轮无关);⑥ **重启**:清 `.next` → `npx next dev -p 3000` (Ready in 1777ms) | 用户端到端验证:① 概念图阶段→悬停某张图→点击"重做"→终端出现 `[CONCEPT-REGENERATE]`→刷新确认新图;② 人物设计阶段→同理验证单条重做;③ 风格统一阶段→同理验证;④ 分镜设计阶段→点击某行↻→重新生成SVG草图;⑤ 各阶段底部"重新生成全部"→二次确认→观察 force=true 日志→刷新确认全部更新 |
|:---|:---|:---|:---|:---|
| 2026-05-18 | 分镜设计卡片修改/拖拽持久化修复（Phase 1-6 全部执行） | `app/(dashboard)/project/[id]/storyboard/page.tsx`（新增 `useEffect`+`hasSynced` 标志,SWR 数据首次到达时同步到 `localShots`;移除 `effectiveShots` 双源逻辑,`localShots` 为唯一数据源）, `app/(dashboard)/project/[id]/storyboard/_components/StoryboardTable.tsx`（新增 `projectId` prop;新增 `saveShots()` 调用 `PATCH /api/projects/{id}/steps/storyboard`;`handleDragEnd`/`handleUpdateShot`/`handleDeleteShot` 后均触发保存并打印 `[STORYBOARD-SAVE]` 日志）, `app/api/projects/[id]/steps/storyboard/route.ts`（新增 `PATCH` handler:校验 `body.shots` 数组→读取 `workflowStep`→更新 `outputData.shots`→返回 `{success:true}`;保留 OWNER/DEMO 鉴权）, `工作指令.txt`（追加 Phase 6 完成标记+Phase 7 开始标记）, `.kimi/work-log.md`（追加本轮记录） | ① **根因 A**:page.tsx `effectiveShots = shots.length > 0 ? shots : localShots`,SWR 原始数据永远优先于本地修改,导致输入框/下拉修改后页面仍显示旧值;② **根因 B**:无 PATCH API,`StoryboardTable` 的 `onShotsChange` 仅更新父组件 `localShots`,无法持久化到数据库;③ **修复策略**:SWR 首次同步→本地状态为唯一数据源+每次变更后 PATCH 保存;不改动 `workflowStep.outputData` 的 JSON 结构,shots 继续内嵌存储（无需新增 Prisma 模型）;④ **诊断日志**:`[STORYBOARD-SAVE] 保存 shots, 数量: N` + `[STORYBOARD-SAVE] API 响应: 200`,便于终端自测验证;⑤ **重启验证**:清 `.next` 缓存 → `npx next dev -p 3000` (Ready in 1878ms,零编译错误) | 用户端到端验证:① 进入分镜编辑器→修改描述/运镜/时长→失焦或回车→终端出现 `[STORYBOARD-SAVE]`→刷新页面确认保持;② 拖拽卡片到新位置→松手→终端出现 `[STORYBOARD-SAVE]`→刷新页面确认顺序保持;③ 边界:拖拽第一个到最后一个、最后一个到第一个、修改后立即拖拽 |
|:---|:---|:---|:---|:---|
| 2026-05-19 | Mock Prisma 持久化修复（解决项目重启后丢失） | `lib/prisma.ts`（新增 `fsSync`/`path` 导入;新增 `DB_FILE=public/mock-storage/db.json`;新增 `saveMockStore()`/`loadMockStore()`/`ensureDbDir()` 三函数;启动时自动从 JSON 恢复数据;所有 7 个写操作方法(create/createMany/upsert/update/updateMany/delete/deleteMany)返回前调用 `saveMockStore()`;加载标志 `g.__mockDbLoaded` 防重复恢复）, `scripts/test-mock-persistence.ts`（新建:持久化测试脚本,验证创建→db.json 生成→模拟重启→数据恢复全流程）, `工作指令.txt`（追加 Phase 1-6 执行标记+排查结论）, `.kimi/work-log.md`（追加本轮记录） | ① **根因确认**:Phase 1-4 排查确认 `lib/prisma.ts` 是 Mock Prisma(DATABASE_URL 为占位符),数据存在 `globalThis.mockStore`(内存),dev server 重启或 Next.js HMR 后内存清空导致项目消失;② **修复策略**:给 Mock Prisma 增加本地 JSON 文件持久化,写操作后同步保存,启动时自动恢复;不改动 Dashboard 查询条件(原本就是正确的);③ **测试通过**:`npx tsx scripts/test-mock-persistence.ts` 验证:创建项目→db.json 存在且含 1 条 project 记录→模拟重启(清内存+重导入)→项目数仍为 1;④ **副作用**:db.json 为明文 JSON,仅用于本地 Demo 开发,不上传到生产环境 | 接入真实 Supabase PostgreSQL 后,`isPlaceholderUrl()` 返回 false,自动切换到真实 PrismaClient,db.json 不再使用;届时需安装 `@prisma/adapter-pg` 并按 prisma.ts 注释配置 |
| 2026-05-18 | 千问百聆替代 MiniMax（代码就绪 + DashScope 账号权限未开通） | `lib/api-clients/dashscope.ts`（新建:`generateMusicQwen`,POST `dashscope.aliyuncs.com/api/v1/services/audio/music/generation`,模型 `fun-music-v1`,120s 超时,解析 `output.audio_url`+`request_id`）, `lib/api-clients/mock-video.ts`（import 新增 `generateMusicQwen` from `./dashscope`;BGM 段重写为三级降级链:千问百聆→MiniMax(url→hex)→30s 静音 AAC;`overallMood` 提取到外层避免重复定义）, `.env.local`（新增 `DASHSCOPE_API_KEY=sk-e2a67ae4c8204fd385c6c81a87355f85`）, `scripts/test-qwen-music.ts`（新建:独立测试脚本,`--yes` 确认守卫,自动下载音频到 `.test-output/`）, `工作指令.txt`（追加 Phase 4/1/2/5 执行标记+测试结果摘要）, `.kimi/work-log.md`（追加本轮记录） | ① **代码侧全部就绪**:`generateMusicQwen` + mock-video.ts BGM 调用 + 测试脚本均已完成;② **DashScope 权限未开通**:POST 返回 `403 Forbidden` + `code: AccessDenied` + `request_id: d77d91a6-1e97-9371-bf20-7b79f7c207c1`;阿里云帮助文档提示需开通模型调用权限;③ **BGM 三级降级链生效**:千问百聆失败→自动 fallback MiniMax→再失败则 30s 静音 AAC,宣传片流程不中断;④ **测试脚本安全**:默认 dry-run,必须 `--yes` 或 `QWEN_TEST_CONFIRM=1` 才真提交;⑤ **TS 编译**:dashscope.ts 零错误,mock-video.ts 剩余 6 个历史 `this` 隐式 any 错误与本轮无关 | 登录阿里云 DashScope 控制台开通 `fun-music-v1` 模型权限;或确认 API Key 所属账号已实名认证并激活音乐生成服务;开通后 `npx tsx scripts/test-qwen-music.ts --yes` 自动验证 |
| 2026-05-18 | MiniMax 音乐生成替代 Suno（代码全就绪 + 供应商端无音乐模型） | `lib/api-clients/xiaomi.ts`（新增 `generateMusicMinimax`/`decodeMinimaxHexToFile`/`MinimaxMusicError`,POST `/v1/music_generation`,支持 url/hex 双格式,120s 超时,全部错误码可读提示）, `lib/api-clients/mock-video.ts`（import 新增 `generateMusicMinimax, decodeMinimaxHexToFile`;BGM 段删除 Suno 旧逻辑(deepseek→lyrics/title/tags→submit/poll/download)→改为 MiniMax 同步调用:prompt 拼接(overallMood+storyBrief.slice(0,300))→优先 `outputFormat='url'`→fallback `hex`→fallback 静音）, `scripts/test-minimax-music.ts`（新建:独立测试脚本,`--yes` 确认守卫,自动 url→hex fallback）, `scripts/probe-minimax-music-endpoint.ts`（新建:探测真实路由,覆盖 7 个候选路径）, `scripts/probe-models.ts`（新建:拉取供应商 453 模型列表,过滤音乐相关）, `scripts/probe-models-filter.ts`（新建:精细过滤 music-2.6/suno/chirp/cover/song）, `scripts/probe-music-alt.ts`（新建:探测 10 个替代音乐 endpoint）, `工作指令.txt`（追加 Phase 1/2/4/5 执行标记+探测结果摘要）, `.kimi/work-log.md`（追加本轮记录） | ① **代码侧全部就绪**:Phase 1 `generateMusicMinimax` 函数+Phase 2 测试脚本+Phase 1.2 mock-video.ts BGM 段替换全部完成;② **供应商无 MiniMax 音乐路由**:POST `/v1/music_generation` → 404 Invalid URL;POST `/minimax/v1/music_generation` → 200 HTML(SPA fallback,非 API);POST `/minimax/v1/music` → 200 HTML(同上);③ **模型列表确认**:供应商 `/v1/models` 共 453 模型,零个匹配 `music-2.6` / `suno` / `chirp` / `cover` / `song`;全部 20 个"音乐相关"模型实为文本/语音/音频预览模型,无音乐生成;④ **Suno 旧路径仍 503**:`/suno/submit/music` 返回 `suno_music 无可用渠道(distributor)`,渠道未恢复;⑤ **BGM 降级链**:MiniMax url→MiniMax hex→30s 静音 AAC,即使供应商未开通也能保证宣传片有音频;⑥ **测试脚本安全**:默认 dry-run,必须 `--yes` 或 `MINIMAX_TEST_CONFIRM=1` 才真提交,避免误跑 | 联系供应商开通 `music-2.6-free` / `music-2.6` 模型接入;或提供正确的路由路径+模型名;供应商开通后 `npx tsx scripts/test-minimax-music.ts --yes` 自动验证 |
| 2026-05-18 | 测试视频/音乐生成 + 防重复提交修复（4 次 Veo 任务根因 + 服务端原子化抢锁） | `lib/workflow-executor.ts`（新增 `tryStartStep(stepId)` 用 `updateMany` 做 WHERE status∈{PENDING,FAILED} 条件更新,返回 count===1 表示抢锁成功;原 `startStep` 保留,只在 trailer 路由替换）, `app/api/projects/[id]/steps/trailer/route.ts`（导入并使用 `tryStartStep` 替代 `startStep`,失败时返回 success+status=PROCESSING 与现有 PROCESSING 分支一致）, `scripts/test-veo-api.ts`/`scripts/probe-veo-query.ts`/`scripts/test-suno-api.ts`（顶部加 `--yes`/`*_TEST_CONFIRM=1` confirm guard,默认 dry-run 防止误跑消耗供应商额度）, `scripts/test-suno-api.ts`（新建:Suno submit/poll/wav 三层独立测试脚本） | ① **Veo 测试再次通过**:`...lM2mfpx8KC` 状态进展 pending → video_generating → completed,最终视频 `https://pro.filesystem.site/cdn/20260518/a611c405471e75b6935848eedc2733.mp4`;② **Suno 仍 503**:`分组 ... 下模型 suno_music 无可用渠道（distributor）` request id `20260518123352964924984IFfHHCPt`,供应商端渠道未挂载,代码已就绪;③ **4 次 Veo 任务根因**:`test-veo-api.ts` 每跑一次 submit 1 个真实任务 + `probe-veo-query.ts` 也会先 submit 1 个真任务再探测查询接口,2 次会话累计跑了 3-4 次脚本就消耗了 4 个供应商额度;④ **正式管线 race condition**:trailer route.ts 第 119/124 行 `step.status === 'PROCESSING'` 检查与 `startStep` 之间无事务,两个并发 POST 都可能读到 PENDING 然后双重启动 setImmediate;新的 `tryStartStep` 用 `updateMany` 的 affected count 做原子化锁,即使前端误点 N 次,服务端只有第 1 次返回真正启动,其它都返回 PROCESSING;⑤ **测试脚本 guard**:三个脚本(test-veo / probe-veo / test-suno)都接入 `--yes`/`VEO_TEST_CONFIRM=1`/`SUNO_TEST_CONFIRM=1`,默认 dry-run 时只打印警告 + 退出,验证 `npx tsx scripts/test-veo-api.ts` 无 `--yes` 直接退出无副作用 | 等 Suno 供应商开通分销渠道后跑 `npx tsx scripts/test-suno-api.ts --yes` 验证;前端 `executeStep` closure 中的 `executing` 旧值校验可作为锦上添花,但已被服务端 `tryStartStep` 兜住,不再是关键路径 |
| 2026-05-17 | 新工作指令：Veo 替代 Hailuo + Suno 查询/下载接口补全（Phase 1-4 全部执行,Veo 端到端验证通过） | `lib/api-clients/xiaomi.ts`（新增 `submitVeoVideo`/`pollVeoTask`/`generateVeoVideo` 三件套,localhost URL 自动转 Base64;`generateVideoFromImage` 优先匹配 `veo3-*` 走新协议;`pollSunoTask` 改为路径式 `/suno/fetch/{task_id}` 为主 + 旧 query 式为 fallback;新增 `pollSunoTaskFull` 返回 { clipId, audioUrl };新增 `downloadSunoWav` 调用 `/suno/act/wav/{clip_id}`）, `lib/models-config.ts`（VIDEO_MODELS.trailer.primary='veo3-fast-frames', fallback='jimeng-video'）, `scripts/test-veo-api.ts`（新建：独立测试 Veo submit + poll,支持 Base64 + 公网 URL）, `scripts/probe-veo-query.ts`（新建：枚举 24+ 候选查询接口探测真实 URL）, `工作指令.txt`（追加 Phase 1/2/3/4 开始与完成标记） | ① **Veo 端到端验证通过**：POST /v1/video/create 提交 200 OK,Base64 Data URL 被接受;状态进展 pending → image_downloading → video_generating → video_generation_completed → video_upsampling → completed,约 2 分 15 秒完成,最终拿到 https://pro.filesystem.site/cdn/.../...mp4;② **真实查询接口**：文档建议的 `/v1/video/{id}` / `/v1/video/query/{id}` / `/v1/video/status/{id}` 全部 404,实测有效接口为 `GET /v1/videos/{id}`（复数）和 `GET /v1/video/query?id={id}`,已固化到 pollVeoTask;③ **Suno 改造**:pollSunoTask 优先 `/suno/fetch/{task_id}` 路径式,新增 pollSunoTaskFull 暴露 clipId 供 wav 下载;downloadSunoWav 实现 `GET /suno/act/wav/{clip_id}` → 返回 wav 文件 URL;④ **当前 Suno 渠道仍 503**:API 密钥无 suno_music 分销,代码已就绪,等渠道开通后即可端到端走通;⑤ **重启**:taskkill PID 17356 → rm -rf .next → `npx next dev -p 3000`（Ready in 1640ms,HTTP 200） | 用户端到端验证宣传片管线:点击"生成宣传片",观察 `[VEO-SUBMIT]` / `[VEO-POLL]` 日志链,6 段视频应全部为 AI 真实视频而非 Mock;Suno 渠道开通后 BGM 也能换为真实音乐 |
| 2026-05-18 | 新工作指令 Phase 1-8 全部执行：移除 isInternalUrl 阻塞 + 创建独立测试脚本验证 API | `lib/api-clients/mock-video.ts`（移除 `isInternalUrl` 检测及条件中的 `!isInternalUrl`，允许 localhost URL 通过 API 调用，Hailuo 有 `resolveImageToBase64` 转 Base64），`lib/api-clients/xiaomi.ts`（`pollHailuoTask` 新增 `file_id` 日志记录，因代理返回 `file_id` 而非 `video_url`），`scripts/test-trailer-api.ts`（新建：独立测试 Hailuo + Suno API 连通性，含 submit + poll），`工作指令.txt`（追加 Phase 1/7/8 执行标记），`.kimi/work-log.md`（追加本轮记录） | ① **核心根因**：Phase 3 的 `isInternalUrl` 修复过度，阻止了所有 API 调用。Hailuo 的 `submitHailuoVideo` 有 `resolveImageToBase64` 可将 localhost URL 转 Base64，不应被阻止；② **Hailuo 测试结果**：提交成功（`status_code: 0`）、轮询到 success，但代理返回 `file_id` 而非 `video_url`，无标准文件下载端点（供应商侧限制）；③ **Suno 测试结果**：HTTP 503 "无可用渠道"，API Key 分组未配置 suno_music distributor（供应商侧配置问题）；④ **重启**：清 .next → `npx next dev -p 3000`（HTTP 200） | 联系供应商开通 Suno 渠道或更换 API Key；确认 Hailuo 视频下载机制（file_id 如何获取视频文件） |
| 2026-05-17 | Phase 5：自测验证 + TS 编译修复 | `lib/api-clients/mock-video.ts`（`generateDirectVideo` catch/finally 分支中 `fs.unlink` 改为 `fsPromises.unlink`，因 `fs` 模块未导入，`fs/promises` 已作为 `fsPromises` 导入），`工作指令.txt`（追加 Phase 5 自测验证完成标记 + 全部修复汇总），`.kimi/work-log.md`（追加 Phase 5 记录） | ① **TS 编译错误**：`generateDirectVideo` 函数末尾使用 `fs.unlink` 但未导入 `fs` 模块（顶部导入的是 `fsSync` 和 `fsPromises`），导致 `TS2304: Cannot find name 'fs'`；② **修复**：改为 `fsPromises.unlink`（异步操作，与上下文 `await` 一致）；③ **构建验证**：`npx next build` 编译成功（exit code 0）；④ **重启**：清 .next 缓存 → `npx next dev -p 3000`（HTTP 200） | 用户端到端验证完整工作流 |
| 2026-05-17 | Phase 4：Suno 音乐生成流程完整性审查 | 无修改，`工作指令.txt`（追加 Phase 4 检查完成标记），`.kimi/work-log.md`（追加 Phase 4 记录） | ① Suno submit/poll/generateMusic 三层函数均已存在（Phase 1 修复 URL/header/body）；② BGM 流程完整：deepseek 生成 lyrics/title/tags → `generateMusic()` → 下载 → ffmpeg 混音；③ 失败回退：任意环节 catch → `generateSilentBgm` 30s 静音 AAC；④ `musicIsMock` 端到端透传；⑤ 结论：Phase 4 无需额外修改 | 用户端到端验证 |
| 2026-05-17 | Phase 3：所有片段都是 Mock 根因修复（localhost URL 外部 API 不可达） | `lib/api-clients/mock-video.ts`（`generateOneSegment` 新增 `isInternalUrl` 检测：识别 localhost/127.0.0.1 URL，匹配时跳过真实 API 直接走 Ken Burns；新增诊断日志 `[TRAILER-SEG] URL 为 localhost/127.0.0.1 内部地址，外部 API 无法访问，走 Ken Burns`），`工作指令.txt`（追加 Phase 3 修复完成标记），`.kimi/work-log.md`（追加 Phase 3 记录） | ① **根因**：DEMO 模式下 concept.url = `/mock-storage/...` → `resolveImageUrl` 拼接为 `http://localhost:3000/mock-storage/...` → 通过了 `isHttpUrl = /^https?:\/\//i.test()` 检测 → 代码进入真实 API 调用路径 → 发送给 Jimeng/Hailuo 供应商 → 供应商服务器无法访问我们本地的 localhost → 请求失败 → catch fallback 到 Ken Burns → 所有片段显示"Mock"；② **修复策略**：在 `generateOneSegment` 的 API 调用条件中增加 `!isInternalUrl` 检查，localhost/127.0.0.1 URL 直接走 Ken Burns，避免无效请求；③ **重启**：清 .next 缓存 → `npx next dev -p 3000`（Ready in ~2s，HTTP 200） | 用户端到端验证：DEMO 模式下所有片段仍为 Mock（预期行为）；接入真实 R2 后 concept.url 为公网 URL → `isInternalUrl = false` → 自动走真实 Jimeng/Hailuo API |
| 2026-05-17 | Phase 2：Hailuo duration 适配 + resolveImageToBase64 localhost 处理 | `lib/api-clients/xiaomi.ts`（`submitHailuoVideo` duration 适配：非 10 的值自动转为 6，因为 Hailuo 仅接受 6/10，而 models-config 中 trailer.duration=5；新增 `[HAILUO-SUBMIT] duration 适配` 诊断日志；`resolveImageToBase64` 新增 localhost/127.0.0.1 URL 处理：检测后直接从 `public/` 目录读取文件转 Base64，不再对 dev server 自身 fetch → 避免潜在失败），`工作指令.txt`（追加 Phase 2 修复完成标记），`.kimi/work-log.md`（追加 Phase 2 记录） | ① **Duration 根因**：`models-config.ts` 设置 `trailer.duration = 5`（Jimeng 的 5s 片段），但 Hailuo 的 duration 字段仅接受 integer 且值必须为 6 或 10；代码中 `duration: params.duration ?? 6` 当传入 5 时仍发送 5 → 供应商拒绝；② **Base64 localhost 根因**：DEMO 模式下 concept.url 为 `/mock-storage/...` → `resolveImageUrl` 拼接为 `http://localhost:3000/mock-storage/...` → `resolveImageToBase64` 走 `fetch()` 分支，对 dev server 自身发起请求可能失败；参考之前 `downloadUrlToTemp` 的成功经验，改为 URL 解析 pathname → `fs.readFileSync` 直读；③ **重启**：清 .next 缓存 → `npx next dev -p 3000`（Ready in 1678ms，HTTP 200） | 用户端到端验证：点击"生成宣传片"，观察 `[HAILUO-SUBMIT] duration 适配: 5 → 6` 日志 → `[HAILUO] first_frame_image Base64 长度:` 确认转换成功 → 供应商日志 MiniMax-Hailuo-02 显示"消费"而非"错误" |
| 2026-05-17 | Suno 音乐生成修复：查询接口路径更正 + 请求体补全 + 状态适配 | `lib/api-clients/xiaomi.ts`（`pollSunoTask` 查询接口从 `/suno/query/music` 更正为 `/suno/fetch`；`submitSunoMusic` 新增 `make_instrumental` 参数和 `Accept: application/json` header；状态判断增加大写 `FAILURE` 适配 + `statusRaw` 原始值日志；`generateMusic` 透传 `makeInstrumental`；`submitHailuoVideo` 新增 `Accept: application/json` header），`工作指令.txt`（追加 Phase 1 Suno 修复完成标记 + 执行前状态检查） | ① **根因**：Suno 供应商日志完全无请求记录，排查发现 `pollSunoTask` 查询接口路径 `/suno/query/music` 与文档 `/suno/fetch` 不一致，可能导致 submit 成功后查询永远 404/失败；`submitSunoMusic` 请求体缺少 `make_instrumental` 字段，BGM 纯音乐场景应显式设为 `true`；② **修复策略**：按 Suno 参考文档逐项核对 — URL 路径 / header / body 三维度对齐；状态判断增加 `statusRaw` 保留原始大小写便于日志排查，`toLowerCase()` 兼容大小写两种格式；③ **Hailuo 同步加固**：补充 `Accept: application/json` header，与 Suno 保持一致 | 用户端到端验证：点击"生成宣传片"，观察终端 `[SUNO-SUBMIT]` / `[SUNO-POLL]` 日志链 → 供应商日志出现 Suno 请求记录 → BGM 显示"Suno 生成"而非"占位静音" |
| 2026-05-17 | Round 15 Phase 1+2：修复图片存储路径错位 + Hailuo 请求补全必需字段 | `lib/api-clients/index.ts`（4 个图像生成方法 storageKey 从 `projects/temp/*` 改为 `projects/${projectId}/*`：`generateStyleSamples` → `projects/${projectId}/styles/`，`generateCharacterPortrait` → `projects/${projectId}/characters/`，`generateConceptScene` → `projects/${projectId}/concepts/`，`generateKeyframe` → `projects/${projectId}/keyframes/`），`lib/api-clients/xiaomi.ts`（`submitHailuoVideo` 恢复必需字段：`first_frame_image` 通过 `resolveImageToBase64` 转 Base64 Data URL，新增 `prompt_optimizer: true`、`fast_pretreatment: true`、`callback_url: ''`、`aigc_watermark: false`），`工作指令.txt`（追加 Round 15 执行标记） | ① **Phase 1 根因**：index.ts 图片客户端使用硬编码 `projects/temp/*` 路径，但 character/concept route 的 Asset storageKey 使用 `projects/{projectId}/*`；DEMO 模式下 uploadFile 写入 `public/mock-storage/<key>`，导致文件存到错误目录，后续宣传片读取时 ENOENT；② **Phase 2 根因**：Round 13 将 Hailuo 请求体最小化为 4 字段后被供应商拒绝（0 token），API 端点需要完整必需字段；`first_frame_image` 必须为 Base64 Data URL 格式（resolveImageToBase64 支持 http(s)/mock-storage/data: 三种来源自动转换）；③ **重启**：清 .next 缓存后重启 dev server | 用户端到端验证：完成完整工作流（创意扩散→框架搭建→风格统一→人物设计→概念图→宣传片），观察 `public/mock-storage/projects/{projectId}/` 下正确生成 styles/characters/concepts/ 子目录；Hailuo 请求 `[HAILUO-SUBMIT]` 日志显示完整字段 |
| 2026-05-17 | 修复 mock-video.ts 重复导入导致编译失败（`the name 'fsSync' is defined multiple times`） | `lib/api-clients/mock-video.ts`（删除第 94 行重复的 `import fsSync from 'fs'` 和第 96 行重复的 `import fs from 'fs/promises'`；将所有 `await fs.` 异步调用替换为 `await fsPromises.`，使用顶部 ideation 拦截器已导入的 `fsPromises`；`resolveFfmpegPath` 继续使用 `fsSync.existsSync`）, `工作指令.txt`（追加 Phase 1 执行完成标记） | ① **根因**：上一轮修复 `fs.promises.existsSync` 时新增的 `import fsSync from 'fs'` 与顶部 ideation 拦截器已有的同名导入冲突，同时 `import fs from 'fs/promises'` 与 `import fsPromises from 'fs/promises'` 也重复；② **修复策略**：删除重复导入，异步操作统一使用顶部 `fsPromises`（`fs/promises`），同步操作使用顶部 `fsSync`（`fs`），不引入任何新的 import 名；③ **重启**：taskkill PID 26428 → `rm -rf .next` → `npx next dev -p 3000`（PID 6872，Ready in 1911ms，零编译错误） | 用户端到端验证：浏览器刷新 → `localhost:3000/project/new` 不再 500 → 点击"生成宣传片"观察完整流程 |
| 2026-05-17 | 修复 fs.promises.existsSync 错误（resolveFfmpegPath 使用了错误的 API） | `lib/api-clients/mock-video.ts`（新增 `import fsSync from 'fs'`，将 `resolveFfmpegPath` 中所有 `fs.existsSync` 改为 `fsSync.existsSync`，因 `fs` 来自 `fs/promises` 异步模块，无 `existsSync` 同步方法）, `工作指令.txt`（追加执行日志: Phase 1+2+3+4 标记） | ① **根因**：`mock-video.ts` 第 95 行 `import fs from 'fs/promises'`，`fs/promises` 是纯异步模块，没有 `existsSync`；上一轮修复时误用了 `fs.existsSync` 导致运行时 `TypeError: fs.existsSync is not a function`；② **修复**：新增 `import fsSync from 'fs'`，所有 `existsSync` 调用改为 `fsSync.existsSync`；③ **video-utils.ts 无此问题**：因使用 `import * as fsSync from 'fs'` 直接引入同步模块 | 用户端到端验证：点击"生成宣传片"，观察 `[MOCK-VIDEO FFMPEG] 使用路径:` 日志 → deepseek-chat 提示词生成 → Hailuo/Ken Burns → 30s 成片 |
| 2026-05-17 | Phase 1+2 修复：Hailuo 请求参数最小化 + ffmpeg 路径多层兜底 | `lib/api-clients/xiaomi.ts`（`submitHailuoVideo` 移除 first_frame_image/prompt_optimizer/fast_pretreatment/callback_url/aigc_watermark 5 个可选字段，仅保留 model/prompt/duration/resolution 4 个供应商平台验证成功的必需字段）, `lib/video-utils.ts`（新增 `resolveFfmpegPath()` 6 层兜底: ffmpeg-static → require.resolve → node_modules 直找 → 系统 PATH → 项目根目录 → 直接调用）, `lib/api-clients/mock-video.ts`（同步引入 `resolveFfmpegPath()` 替代简单 `ffmpegStatic || 'ffmpeg'`，与 video-utils 一致）, `工作指令.txt`（追加执行日志: Phase 1+2 已完成标记） | ① **Hailuo 根因**：供应商平台仅传 4 字段返回 200，代码额外传 5 字段导致"请求失败"；采用最小化请求体策略，与供应商平台测试一致；② **ffmpeg 根因**：Next.js 打包后 ffmpeg-static 路径被解析到 `.next/server/vendor-chunks/`，该目录无 ffmpeg.exe；6 层兜底确保开发/生产/系统安装/本地文件都能找到有效路径；③ **重启**：taskkill PID 25624 → `rm -rf .next` → `npx next dev -p 3000`（PID 25788，Ready in 2.2s） | 用户端到端验证：① Hailuo 修复验证 — 切换模型到 minimax-hailuo，观察 `[HAILUO-SUBMIT]` 日志确认仅传 4 字段 → 供应商日志应显示"消费"而非"请求失败"；② ffmpeg 修复验证 — 触发 Ken Burns，观察 `[FFMPEG] 使用路径:` 日志指向有效 ffmpeg.exe → seg_00.mp4 成功生成 |
| 2026-05-11 | Round 14：宣传片 downloadUrlToTemp 404 修复（localhost URL 直读 + URL 诊断日志） | `lib/video-utils.ts`（`downloadUrlToTemp` 新增 localhost/127.0.0.1 分支:用 `new URL()` 解析 pathname → `fs.readFile` 直读 `public/` 下文件,避免对 dev server 静态路径 fetch → 404）, `lib/api-clients/mock-video.ts`（`generateTrailer` 中 concepts 加载后立即打印 URL 诊断: index/url/urlLength/urlLast20 + invalidConcepts 过滤;`loadConceptsAndFramework` 中每 asset 打印 key/assetUrl/finalUrl 来源追踪）, `工作指令.txt`（追加 Round 14 执行盘点 + 5 项即将执行/完成 标记） | ① **根因**：`resolveImageUrl` 把 `/mock-storage/...` 拼接为 `http://localhost:3000/mock-storage/...`，`downloadUrlToTemp` 对该 URL 做 `fetch()` → Next.js dev server 无法在 `/mock-storage/...` 路由提供静态文件 → 404；② **修复三(downloadUrlToTemp)**：检测 URL 含 `localhost`/`127.0.0.1` → 解析 pathname → `fs.readFile` 直读，完全绕过 fetch；③ **修复一+四(诊断日志)**：generateTrailer 打印 concepts URL 四字段 + invalidConcepts 过滤，loadConceptsAndFramework 打印每 asset 的 key/assetUrl/finalUrl 来源追踪，方便定位截断根因；④ **修复五(Ken Burns)**：通过修复三已覆盖，downloadUrlToTemp 现在正确处理 localhost URL | 用户端到端验证:点击宣传片"生成/重试",观察 `[TRAILER] concepts URL 诊断:` 日志确认 URL 完整 → `[TRAILER-ASSET]` 确认来源 → `[DOWNLOAD] 本地直读:` 成功找到文件 → Ken Burns 生成 6 段 → concat → 30s 成片 |
| 2026-05-11 | Round 11：工作指令.txt 5 项修复盘点 + 补修复四/五（环境变量 + 前端 FAILED 详情） | `.env.local`（追加 `NEXT_PUBLIC_APP_URL="http://localhost:3000"` + 注释,用于未来 URL fetch 兜底）, `app/(dashboard)/project/[id]/workflow/page.tsx`（TrailerPanel 新增 `step.status === 'FAILED'` 分支:红色错误块 + `<details>` JSON + 红色重试按钮）, `工作指令.txt`（追加 Round 11 进度标记:5 项修复盘点 + 修复四/五 即将执行/完成 三段标记） | ① **5 项修复盘点**:修复一(URL 解析) Round 9 采用 `fs.readFile` 直读 `/mock-storage/`(更优于 spec 的 `resolveImageUrl`);修复二(ffmpeg-static)/三(防御日志) Round 10 完成;修复四/五本轮补;② **修复四(NEXT_PUBLIC_APP_URL) 行为**:当前 mock-video.ts 不读该变量(走 fs.readFile),但为未来代码可能改回 URL fetch 保留兜底,值定为 3000(dev server 实际端口,非 spec 写的 3001);③ **修复五(TrailerPanel FAILED)**:原代码 `step.status === 'FAILED'` 直接返回 null,用户看不到失败原因;现在三层展示(errorMessage 单行 + outputData JSON 折叠 + 重试按钮),与 IDEATION/STYLE 的失败 UI 风格统一;④ **过程标记规则**:每次"即将执行"前先在 工作指令.txt 加 `⏳` 段,完成后改/补 `✅`,留下完整审计链;⑤ **重启**:taskkill PID 33468 → `rm -rf .next` → `npm run dev` 后台 → 验证 LISTENING + HTTP 200 | 用户端到端验证:① 浏览器进入宣传片步骤,点击"生成宣传片",观察生成成功路径仍正常;② 制造一次失败场景(如临时改坏 ffmpegPath)验证 FAILED 分支:errorMessage + JSON 折叠 + 重试按钮三件套都能展示 |
| 2026-05-11 | Round 10：工作指令.txt 五部分修复（ffmpeg-static 集成 + 详细错误捕获 + 结构化 500） | `lib/api-clients/mock-video.ts`（import alias 由 `ffmpegPath` 改为 `ffmpegStatic`，模块加载即 `console.log('[MOCK-VIDEO] ffmpeg 路径:', ...)`）, `lib/video-utils.ts`（新增 `runFfmpeg(stage, cmd, maxBuffer)` 统一入口，四处 execAsync 全部改走该入口：`kenBurns / concat / silentBgm / mix`，失败打印 `[MOCK-VIDEO] <stage> ffmpeg 失败 + stderr`），`app/api/projects/[id]/steps/trailer/route.ts`（inline catch 写入 `err.message + err.stack.slice(0,200)` 到 `step.errorMessage`；POST handler 同步阶段整体 try/catch，意外抛错返回 `{ error, detail }` + 500），`工作指令.txt`（追加本轮 5 部分执行进度标记 + 验收完成情况） | ① **真核心改动**：工作指令.txt 顶部 5 部分中，1/3 已在 Round 9 完成（导入 + quote 路径），本轮重点补 2/4/5：`console.log` 路径 + `runFfmpeg` 统一错误捕获 + POST 同步异常结构化 500；② **runFfmpeg 抽象的价值**：4 处 execAsync 复用同一套 stage 名称 + stderr 捕获 + 统一异常前缀，未来加 ffmpeg 命令直接 `runFfmpeg('xxx', cmd)` 即可；③ **POST 500 的意义**：原代码鉴权/Prisma 同步异常会让 Next.js 默认返回通用 500 + HTML，前端只能看到 "Failed to fetch"；现在统一返回 JSON `{ error, detail }` 后前端 toast 可直接显示具体错误；④ **inline catch 的意义**：setImmediate 内 catch 不能返回 500（POST 已 200），但写入 `step.errorMessage` 后 GET /trailer 可读取，前端轮询时 toast 错误详情；⑤ **重启**：taskkill 旧 PID 20584、`rm -rf .next` 清缓存、`npx next dev -p 3000` 后台启动 2.5s ready，curl HTTP 200 验证通过 | 用户端到端验证：浏览器进入宣传片点击"重试"，观察 `[MOCK-VIDEO] ffmpeg 路径:` / `[MOCK-VIDEO] kenBurns(seg_00.mp4) 成功` / `[MOCK-VIDEO] concat(6段→concat.mp4) 成功` / `[MOCK-VIDEO] mix(video+audio→final.mp4) 成功` 日志链，确认 R2 / 前端"已完成" 角标 |
| 2026-05-07 | Round 9：宣传片"长时间无产出"故障自检与修复（按工作指令.txt 四阶段诊断） | `lib/r2.ts`（R2 占位符模式重写：检测 `your-*`/`[account-id]` 自动切到 `public/mock-storage/` 本地兜底；`getSignedFileUrl` 直接返回 `/mock-storage/<key>` 相对 URL；`uploadFile`/`deleteFile` 走 fs.writeFile/unlink）, `lib/api-clients/mock-video.ts`（`downloadKeyToTemp` 识别相对 URL 改为 fs.readFile）, `app/api/projects/[id]/steps/trailer/route.ts`（POST 全链路 [TRAILER-POST] 日志 + processTrailerInline 五件套 [TRAILER-JOB-*]）, `scripts/video-worker.ts`（BullMQ 入口五件套 [TRAILER-JOB-*]）, `lib/api-clients/xiaomi.ts`（submitJimengVideo 扩展为完整五件套 [JIMENG-SUBMIT]）, `public/mock-storage/`（新建目录）, `工作指令.txt`（追加四阶段执行标记 + 最终诊断报告） | ① **真根因**：DEMO 模式下 R2 凭据全为占位符，原 r2.ts 直接调 AWS SDK 必抛 `STORAGE_001`；setImmediate 兜底吞错，前端永远 PROCESSING；② **核心修复**：r2.ts 占位符自动切本地，让 DEMO 模式宣传片端到端走 Ken Burns + 静音 BGM 闭环；③ **诊断日志全覆盖**：[TRAILER-POST] 看请求是否到达 + 走哪条分支；[TRAILER-JOB-*] 五件套同时存在于 BullMQ Worker 与 setImmediate 兜底；[JIMENG-SUBMIT] 五件套覆盖供应商 API 三层信息（endpoint/key/响应）；④ **DEMO 模式自然行为**：concept.url 为 `/mock-storage/...` → mock-video 内 `isHttpUrl` 检测为 false → 自动跳过 Jimeng/Hailuo/Suno 真实 API → 走 Ken Burns + 静音兜底 → 不会有真实供应商记录（这是预期）；⑤ **重启**：清 .next 缓存，taskkill PID 29292，npx next dev -p 3000 重启（PID 34256，Ready in 2.4s，HTTP 200 验证通过） | 真实凭据接入：补全 R2_ACCOUNT_ID/R2_ENDPOINT/UPSTASH_REDIS_URL/DATABASE_URL → r2.ts 自动切回 AWS SDK，concept.url 变成真实 https URL → mock-video 自动调真实 Jimeng/Hailuo + Suno；TODO(round8) 三个 query 路径仍待真实文档校对 |
| 2026-05-07 | Round 8：接入真实 即梦 / 海螺 / Suno 异步任务管线 | `lib/api-clients/xiaomi.ts`（新增 `submitJimengVideo`/`pollJimengTask`/`submitHailuoVideo`/`pollHailuoTask`/`submitSunoMusic`/`pollSunoTask` 6 个底层函数 + `generateMusic` 高层封装；重构 `generateVideoFromImage` 按 model 分流到 jimeng / hailuo / 旧 Veo 三条协议）, `lib/models-config.ts`（VIDEO_MODELS.trailer 切到 `jimeng-video`/`minimax-hailuo`，新增 `MUSIC_MODELS.trailer = { primary: 'suno-chirp-v4', mv: 'chirp-v4', defaultTags: 'cinematic orchestral epic trailer' }`）, `lib/api-clients/video.ts`（TrailerResult 增加 `musicIsMock` 字段）, `lib/api-clients/mock-video.ts`（BGM 阶段：deepseek 生成 lyrics/title/tags JSON → `generateMusic` Suno 真实生成 → 下载本地；任意环节失败自动回退 `generateSilentBgm`，bgmIsMock 透传到 result）, `app/api/projects/[id]/steps/trailer/route.ts`（写入并透传 `musicIsMock`）, `scripts/video-worker.ts`（BullMQ 分支同步透传 `musicIsMock`）, `app/(dashboard)/project/[id]/workflow/page.tsx`（TrailerPanel 主视频下方新增 `<audio controls />` 元素，♪ 已混音角标按 musicIsMock 区分 Suno/占位） | ① **异步轮询接口路径假设**：Jimeng `GET /jimeng/query/videos?task_id=`、Hailuo `GET /minimax/v1/query/video_generation?task_id=`、Suno `GET /suno/query/music?task_id=`，全部代码内 `// TODO(round8)` 注释标注，待真实文档微调；② **降级链**：Jimeng → Hailuo → Ken Burns Mock；Suno → silent BGM；任何环节失败都不阻塞 30s 成片可播；③ **轮询参数**：每段视频 60 × 5s = 5 分钟超时；Suno 60 × 5s = 5 分钟；轮询日志做了 lastSnapshot 去重，避免高频日志噪音；④ **图 URL 预校验**：Jimeng/Hailuo 都要求 `image_url`/`first_frame_image` 是公网可访问的 http(s) URL，client 层显式校验拒绝 data: URL；⑤ **BGM 文件后缀自适应**：从 audio_url 后缀（mp3/wav/m4a/aac/ogg）猜测，决定 R2 存储 key 与 MIME；ffmpeg `mixAudioVideo` 重新编码为 AAC，输入格式不限；⑥ **musicIsMock 端到端**：mock-video → TrailerResult → outputData → GET /trailer → TrailerPanel 角标，Suno/静音双路径用户都可识别 | 待真实供应商文档确认 jimeng/hailuo/suno query 路径；将 `mockMode: true` 切到 `false` 验证真实 Jimeng 出片质量；如果 Jimeng 5s 时长画面不够稳定可切到 Hailuo 6s/1080P |
| 2026-05-06 | Round 7：宣传片完整工作流（图生视频 → 拼接 → BGM → 混音） | `lib/video-utils.ts`（新建）, `lib/api-clients/xiaomi.ts`（新增 `generateVideoFromImage` + 异步任务轮询）, `lib/api-clients/mock-video.ts`（重写 `generateTrailer` 6 步管线）, `lib/api-clients/video.ts`（扩展 `TrailerResult` 含 `segments`/`musicUrl`）, `lib/api-clients/index.ts`（VideoClient 改为 re-export from video.ts）, `lib/models-config.ts`（新增 `VIDEO_MODELS.trailer` 结构 + `TEXT_MODELS.TRAILER_PROMPT`）, `app/api/projects/[id]/steps/trailer/route.ts`（POST 写入完整 `outputData`，GET 透传 segments/musicUrl）, `scripts/video-worker.ts`（BullMQ 分支同步写入 segments/musicUrl）, `app/(dashboard)/project/[id]/workflow/page.tsx`（TrailerPanel 增加 6 段缩略图 + Mock/AI 角标 + ♪ 已混音指示） | ① **图生视频接口**：用 `/v1/videos/generations` + image 字段，自动识别同步/异步返回（异步轮询 `/v1/videos/tasks/{id}` 最多 240s）；② **Mock 兜底策略**：当前阶段 `mockMode: true`，`generateOneSegment` 跳过真实 API 直接 Ken Burns（5s zoompan 缓慢缩放，1920x1080 / 25fps），保证 30s 成片可播；③ **拼接管线**：concat demuxer + 强制重新编码（避免 Veo 与 Ken Burns 输出参数错位）；④ **BGM**：lavfi anullsrc 30s 静音 AAC 占位，后续可替换为真实音乐 API；⑤ **片段持久化**：6 个 5s 片段单独上传 R2 → `projects/{pid}/trailer-segments/seg_N.mp4`，前端 video 标签可直接播放预览缩略图 | 验证完整工作流：概念图 → 宣传片（点击"生成宣传片" → 30s 主视频 + 6 段缩略图渲染 + ♪ 已混音）；后续可关闭 `mockMode` 接入真实 Veo 3.1 Lite |
| 2026-05-04 | 修复四大交互问题：项目列表/提示词JSON化/框架缺失/生图503 | `prompts/*.txt` (7个模板全部JSON化), `lib/prompts.ts` (优先直接JSON.parse), `lib/api-clients/xiaomi.ts` (文本Token提升至12000, 图像增加重试+Mock兜底), `app/(dashboard)/dashboard/page.tsx` (移除userId过滤), `app/api/projects/*/steps/{ideation,framework,character,style,concept,keyframes}/route.ts` (全部改用`extractJsonFromMarkdown`), `components/ui/MarkdownRenderer.tsx` (新增), `app/(dashboard)/project/[id]/workflow/page.tsx` (FrameworkPanel使用MarkdownRenderer) | 所有LLM输出改为严格JSON，前端直接`JSON.parse()`，彻底解决格式错乱和内容缺失；Dashboard查询放宽确保Demo模式下项目必显；图像503自动重试并降级Mock，保证流程不中断；框架搭建页新增"灵感阐释"折叠面板 | 验证完整工作流：新建项目→创意扩散→框架搭建→风格统一→人物设计→概念图 |
| 2026-05-04 | 重构为单一聚合 API 架构（xiaomi-api） | `lib/api-clients/xiaomi.ts`, `lib/models-config.ts`, `lib/api-clients/index.ts` (重写兼容层), `.env.local` (清理旧 Key), 删除 `lib/api-clients/{text,image,mock-text,mock-image}.ts` | 放弃多供应商降级模式，统一接入 `https://vip.123everything.com`；通过 `model` 参数切换底层模型；`index.ts` 保持对外 API 兼容，路由零改动；视频仍走本地 FFmpeg Mock（待 xiaomi-api 视频接口验证） | 测试文本生成（deepseek-chat）和图像生成（flux.1）连通性；验证创意扩散到概念图全流程 |
| 2026-05-01 | Phase 6: 真实 AI API 接入层 + 完整提示词文档系统 | `prompts/*.txt` (9个模板), `lib/prompts.ts`, `lib/api-clients/text.ts`, `lib/api-clients/image.ts`, `lib/api-clients/mock-text.ts`, `lib/api-clients/mock-image.ts`, `scripts/test-apis.ts`, 7个路由文件更新 | 文本API工厂(Kimi/DeepSeek/智谱自动降级)、图像API工厂(硅基流动FLUX/阿里云百炼/百度千帆自动降级)；全部路由改用`loadPromptTemplate`拼接prompts/模板后调用API；测试脚本运行成功，API密钥返回401（已过期占位符），降级机制验证通过 | 更新.env.local中的真实有效API密钥后重新运行测试；或继续Phase 3C/4 |
| 2026-05-01 | Phase 5 后续：Demo 模式补丁（保证无真实 OAuth/数据库也可完整浏览 UI） | `lib/prisma.ts`, `auth.ts`, `middleware.ts`, `app/(dashboard)/layout.tsx` | Prisma 7 强制 driver adapter 而项目未装 → mock Proxy 替代，user.findUnique 返回 demo 用户；OAuth 凭据仍为占位符 → 自动检测 `isDemoMode`，middleware 放行受保护路由，`auth()` 无参数调用直接返回 demo session；`npm run dev` 启动后 `/`, `/login`, `/dashboard`, `/project/new` 全部 200 | 配置真实 Supabase + GitHub OAuth 后删除 demo 短路；Phase 6 接入 `@prisma/adapter-pg` 真正连库 |
| 2026-04-30 | Phase 5: 前端页面与交互系统（全部8模块） | `app/(dashboard)/**` (layout/dashboard/project-new/project-overview/workflow/storyboard/assets), `components/generation/*` (AssetCard/ImageLightbox/VideoPlayer/QueueMonitor), `components/ui/EmptyState`, `app/api/projects/[id]/assets/route.ts`, `components/workflow/IdeaAnchor/StepBadge` | 按模块1→8顺序逐个实现；表格视图用`@dnd-kit/core+sortable`实现拖拽排序；视频用IntersectionObserver懒加载；资产库支持分页+类型筛选+批量下载；队列监控3秒自动消失已完成任务；`npm run dev`编译通过 | Phase 3C: AI渲染+电脑运镜+评测(3.10-3.14)，或Phase 8部署答辩优化 |
| 2026-04-28 | Phase 3B: 影视制作层 Mock 模式 | `lib/api-clients/mock-video.ts`, `lib/api-clients/video.ts`, `scripts/video-worker.ts`, `app/api/projects/*/steps/{trailer,storyboard,representative,keyframes,video-direct}/route.ts`, `app/(dashboard)/project/**` | ffmpeg-static 合成占位视频（30s 宣传片/2s 直生片段）；BullMQ 异步队列验证；分镜 SVG 线稿用颜色区分人物；Worker 脚本独立运行（`npx tsx scripts/video-worker.ts`）；`npm run dev` 编译通过 | Phase 3C: AI 渲染 + 电脑运镜 + 评测 (3.11-3.14)，或 Phase 5 前端优化 |
| 2026-04-28 | Phase 3A: 核心业务引擎 Mock 模式 | `lib/api-clients/**`, `lib/workflow-executor.ts`, `app/api/projects/**`, `app/(dashboard)/**`, `.env.local` 等 | Mock 工厂用 Sharp 生成真实占位图并上传 R2，前端通过预签名 URL 加载，与真实模式体验一致；全部 API 通过 `MOCK_MODE` 开关控制；`npm run dev` 编译通过 | Phase 3B: 分镜 → 直生视频 (3.6-3.9)，或 Phase 5.2/5.3 前端交互优化 |
| 2026-04-28 | Phase 2: 数据模型与认证系统 | `prisma/schema.prisma`, `lib/workflow.ts`, `auth.ts`, `middleware.ts`, `app/api/auth/[...nextauth]/route.ts`, `app/login/page.tsx`, `lib/auth-helpers.ts`, `app/layout.tsx`, `.env.example`, `.env.local` | Prisma 7 禁止在 schema 中写 `url`/`directUrl`，已全部迁移至 `prisma.config.ts`；NextAuth v5 环境变量前缀改为 `AUTH_`；`npm run dev` 编译通过 | Phase 3: 核心后端 API 工作流引擎 (3.1 创意扩散) 或 Phase 5.1 全局布局 |
| 2026-04-28 | Phase 1: 项目骨架与基础设施 | `package.json`, `prisma/schema.prisma`, `prisma.config.ts`, `lib/prisma.ts`, `lib/r2.ts`, `lib/queue.ts`, `lib/utils.ts`, `types/index.ts`, `app/**`, `.env.example`, `.env.local` 等 | 系统无 Node.js，通过 winget 安装；Prisma 7 要求 `prisma.config.ts` 分离连接配置；shadcn/ui 手动配置（stone 主题） | Phase 2: 数据模型与 NextAuth (2.1-2.3) |
| 2026-04-28 | 建立工程文档体系 | `.kimi/` 下 8 个 Markdown 文件 | 确定使用 Next.js 14 App Router + BullMQ 异步架构 | 初始化项目脚手架 (Phase 1.1) |

| 2026-06-07 | 修复风格统一"重试后提示词未生成、生图未调用"问题 | `app/(dashboard)/project/[id]/workflow/page.tsx`（StepHeader onRetry + StylePanel FAILED 重试按钮：无 prompts 时调用 `{ action: 'generate-prompts' }`，有 prompts 时传 `force: true` 清空旧资产并重新生图）, `app/api/projects/[id]/steps/style/route.ts`（提取 `generateStylePrompts` 辅助函数供 generate-prompts 和 generate-images 共用；generate-images 分支 prompts 为空时自动触发提示词生成而非返回 400；force=true 时重新读取最新 outputData 避免覆盖自动生成的 prompts） | ① **根因一（前端）**：FAILED 状态重试且无 prompts 时调用 `executeStep('STYLE')`，body 为空导致后端默认 `action='generate-images'`，而 generate-images 要求 prompts 已存在，直接返回 400 导致流程断裂；② **根因二（后端）**：generate-images 分支 prompts 为空时直接返回 400 `No prompts found`，没有兜底自动触发提示词生成；③ **根因三（数据覆盖）**：force=true 清空旧资产时使用旧的 `existingOutput` 覆盖数据库，若之前自动生成了 prompts 会被覆盖丢失；④ **修复策略**：前端重试分两支 — 无 prompts 调 generate-prompts，有 prompts 调 generate-images+force:true；后端提取共用函数，prompts 为空时自动调用提示词生成再继续生图；force=true 时重新读取最新 outputData；⑤ **关联问题**：CHARACTER/CONCEPT 路由有同样结构（prompts 为空返回 400），待后续统一修复 | 用户端到端验证：创建新项目 → 风格统一步骤制造失败（如断网）→ 点击重试 → 观察 Network 面板确认调用了正确 API → Vercel Logs 确认提示词生成和生图调用都有日志 → 前端最终显示风格样图 |
