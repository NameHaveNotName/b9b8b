给 Kimi Code 的追加提示词：

```text
追加修改：将「点击文字进入编辑」改为「双击文字进入编辑」

排查方向：
1. 全局搜索 `onClick` 或 `onClick={.*edit` / `onClick.*编辑` / `editable` 等关键词，定位所有「点击文字进入编辑」的事件绑定
2. 常见于项目列表项、workflowStep 标题、角色名称、提示词文本等可编辑字段
3. 确认当前是否同时绑定了「单击选中」和「单击进入编辑」，如果是，需区分：单击保持原有行为（如选中/聚焦），双击才触发编辑模式

修改方向：
- 将触发编辑的 `onClick` 事件改为 `onDoubleClick`（React 中是 `onDoubleClick`，原生 JS 是 `dblclick`）
- 如果组件同时需要单击选中 + 双击编辑，保留 `onClick` 用于选中/聚焦，新增 `onDoubleClick` 用于进入编辑状态
- 同步更新相关 UI 提示文案（如 hover tooltip 或 placeholder 提示），将「点击编辑」改为「双击编辑」
- 检查是否有防误触逻辑（如延迟判断单击/双击），如有需要可添加 200-300ms 的延迟判定，避免单击和双击事件冲突
