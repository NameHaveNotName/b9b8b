# 工作流管线审计报告 v2

> 生成时间：2026-05-23
> 审计范围：从用户输入创意到直生视频的全部 9 个可见步骤
> 说明：本报告逐步骤拆解每个子任务，贴出提示词原始模板（文件自带）或指向生成该提示词的上游任务（动态生成），合成结果用"+"号连接中间产物，参考图追溯至具体步骤

---

## 参考图依赖总览

```
步骤 3 STYLE ── 3 张风格图（用户选 1 张）──┐
                                          ├─► 步骤 4 CHARACTER（风格图 1 张，图生图）
                                          ├─► 步骤 5 CONCEPT（风格图 1 张 + 角色图 N 张，多图参考）
                                          └─► 步骤 8 KEYFRAMES（风格图 1 张，图生图）

步骤 7 STORYBOARD ── 分镜表 shots ──┬─► 步骤 8 KEYFRAMES（首帧只读引用，不作生成参考）
                                   └─► 步骤 9 VIDEO_DIRECT（首帧 ffmpeg 输入）

步骤 5 CONCEPT ── 6 张概念图 ─────► 步骤 6 TRAILER（每张作为图生视频首帧）

步骤 8 KEYFRAMES ── 尾帧 N 张 ────► 步骤 9 VIDEO_DIRECT（尾帧 ffmpeg 输入，可选）
```

---

## 步骤 1: IDEATION（创意扩散）

### 子任务清单

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 1-1 | 文本生成 | `TEXT_MODELS.IDEATION`（DeepSeek/Kimi/智谱自动降级） | `project.rawIdea`（用户原始想法） | `directions[3]` + `framework` |

### 提示词

**来源：文件 `prompts/ideation.txt`（直接读取，模板变量替换 `{{USER_INPUT}}`）**

```
角色：高端艺术电影 AI 创意总监

目标：你是一家顶尖艺术电影工作室的创意总监。你的任务是根据客户对故事的印象描述，生成对应的具有"奥斯卡级别"修改后的创意点子的提示词。

要求：
你对提供的点子具有百分百的改动权，包括人物设定，故事背景和故事梗概等等。
根据创新型的思维思考剧情是否足够创新且引人入胜，人设是否足够完善，冲突是否激烈等等。

【强制指令】你必须生成一个包含恰好 3 个创意方向的 JSON 数组。禁止只生成 1 个方向，禁止生成 2 个或 4 个以上方向。每个方向必须在风格、主题或叙事角度上有明显差异。
思考总共30个镜头，整体分为三幕（每一幕镜头数量可以不一样），每个镜头不超过十秒，总共两分钟左右的影片能不能承载下这个故事。
无论什么风格，都需要尽量谨慎地使用特效（包括闪光，火花，全息等），尽量避免出现相关内容。

输出格式（严格 JSON，不要 Markdown 代码块）：
{
  "directions": [
    {"title": "方向标题（20字内）", "description": "方向描述（100字内）", "keywords": ["关键词1", "关键词2", "关键词3"]}
  ],
  "framework": {
    "inspiration": "灵感阐释...",
    "styleGuide": "风格规范...",
    "background": "背景环境设定...",
    "characters": [{"id": "char_001", "name": "角色名", "role": "主角/配角", "description": "性格、形象等基本信息..."}],
    "acts": [{"actNumber": 1, "scenes": ["场景1描述", "场景2描述"]}]
  },
  "rawText": "（可选）完整原文备份"
}

客户提供的灵感内容如下：
{{USER_INPUT}} = project.rawIdea
```

### 调用参数
- `temperature: 0.8`, `maxTokens: 12000`

### 引用图像
无（纯文本步骤）

### 输出产物
- `outputData.directions[3]` — 3 个创意方向
- `outputData.framework` — 包含 characters[], acts[], synopsis, visualStyle

---

## 步骤 2: FRAMEWORK（框架搭建）

### 子任务清单

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 2-1 | 文本生成 | `TEXT_MODELS.IDEATION`（与步骤 1 相同） | `project.rawIdea` + `用户选定的方向 title+description` | 详细 `framework` |

### 提示词

**来源：文件 `prompts/ideation.txt`（与步骤 1 同一文件，但注入的 `{{USER_INPUT}}` 不同）**

```
{{USER_INPUT}} = project.rawIdea + "\n\n用户选定的创意方向：{selectedDirection.title}\n{selectedDirection.description}"
```

模板文件内容与步骤 1 完全一致，区别仅在于用户输入多了选定方向的锚定信息。

### 调用参数
- `temperature: 0.8`, `maxTokens: 12000`

### 引用图像
无（纯文本步骤）

### 输出产物
- `project.framework = { inspiration, styleGuide, background, characters[], acts[], synopsis, visualStyle }`

---

## 步骤 3: STYLE（风格统一）

### 子任务清单

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 3-1 | 文本生成 | `TEXT_MODELS.STYLE` | 步骤 2 FRAMEWORK 输出的 `synopsis + visualStyle + mood` | 3 组风格提示词（styleName + styleDescription + prompt） |
| 3-2 | 图像生成 × 3（并行） | `IMAGE_MODELS.STYLE`（FLUX/阿里云百炼/百度千帆自动降级） | 子任务 3-1 输出的每组 `prompt` | 3 张 16:9 风格样图 |

### 提示词

**子任务 3-1 — 来源：文件 `prompts/style-generation.txt`**

```
角色：高端艺术电影 AI 美术指导

目标：你是一家顶级艺术电影工作室的美术指导。你的任务是根据影片的框架设定（故事梗概、视觉关键词、氛围），生成 3 种截然不同的视觉风格方案，用于确定整部影片的统一视觉基准。

要求：
1. 每种风格必须包含：styleName, styleDescription, prompt
2. 三种风格必须在色彩、光影、构图、质感上形成明显差异
3. 每种风格的 prompt 必须包含统一的故事主体
4. 每张风格图必须是 16:9 横屏构图，且必须包含至少一个角色的人物形象（侧影/背影/局部特写，禁止脸部特写）
5. prompt 中应包含：cinematic film still, 35mm Kodak Portra 400, atmospheric depth, 8K

输出格式（严格 JSON 数组，不要 Markdown 代码块标记）：
[
  {"id": "1", "styleName": "风格名称", "styleDescription": "50字中文描述...", "prompt": "英文图像生成提示词..."},
  {"id": "2", ...},
  {"id": "3", ...}
]

影片信息如下：
故事梗概：{{STORY_BRIEF}} = framework.synopsis || framework.storyBrief
视觉关键词：{{VISUAL_KEYWORDS}} = framework.visualStyle || framework.styleGuide
氛围基调：{{MOOD}} = framework.mood || framework.atmosphere
```

**子任务 3-2 — 提示词 = 子任务 3-1 输出的每组 `prompt` 字段（动态生成）**

无固定后缀，直接使用 LLM 输出的 `prompt` 原文。

### 调用参数
- 文本：`temperature: 0.7`, `maxTokens: 4096`
- 图像：`size: "16:9"`，3 张并行（`Promise.all`）

### 引用图像
无前置引用。本步骤 3 张风格图是 **所有后续图像生成的风格参考源头**。

### 输出产物
- `outputData.styleOptions[3]` — 含 id/styleName/styleDescription/prompt/imageUrl
- `outputData.selectedStyleId` — 用户选定的风格 ID
- `outputData.styleRefUrl` — 选定风格图的 URL

---

## 步骤 4: CHARACTER（人物设计）

### 子任务清单

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 4-1 | 文本生成 | `TEXT_MODELS.characterDesign` | 步骤 2 FRAMEWORK 输出的完整 JSON | 每个角色的英文图像提示词 |
| 4-2 | 图像生成 × N（最多 5 张） | `IMAGE_MODELS.characterDesign.primary` | 子任务 4-1 输出的 `imagePrompt` + 固定后缀 + 步骤 3 风格图（可选） | N 张角色肖像 |

### 提示词

**子任务 4-1 — 来源：文件 `prompts/character.txt`**

```
角色：高端艺术电影 AI 角色设计顾问

目标：你是一家顶级艺术电影工作室的艺术总监。你的任务是根据客户对故事的印象派描述，生成"奥斯卡级"品质的主角图像生成提示词。

全局风格与美学指令（适用于所有镜头）：
视觉风格：大师级商业摄影。35mm 胶片美学，模拟 Kodak Portra 400 胶卷质感，柔和且梦幻的颗粒感。光照：与参考图匹配的自然环境光，在适用处使用逆光或镜头光晕，高动态范围。摄像机：手持悬浮斯坦尼康感，平滑且有机的运动。氛围：空灵、怀旧、亲密、神圣、诗意现实主义、情感共鸣、奥斯卡级调色。

系统指令 AI 规则集：
1. 主角数量判定：一个或两个主角
2. 禁止摆拍：描述动作或情绪状态
3. 窥视视角：过肩镜头/穿过树叶/高角度远景
4. 偏离中心：三分法或留白
5. 禁止剪影：边缘光（Rim lighting），保留织物和皮肤细节
6. 电影质感：像电影剧照，不是照片

输出格式（严格 JSON，不要 Markdown 代码块）：
{
  "characters": [
    {"characterId": "char_01", "name": "角色名", "imagePrompt": "[英文图像提示词]"}
  ]
}

整体灵感内容如下：
{{USER_INPUT}} = JSON.stringify(framework)
```

**子任务 4-2 — 提示词 = 子任务 4-1 输出的 `imagePrompt` + 框架中的 `description`**

```
{character.description} + {charData.imagePrompt}
```

其中 `character.description` 来自步骤 2 FRAMEWORK，`charData.imagePrompt` 来自子任务 4-1。风格通过 `referenceImageUrl`（步骤 3 风格图）注入图像 API，而非拼接到提示词文本中。

### 调用参数
- 文本：`temperature: 0.7`, `maxTokens: 4096`
- 图像：`IMAGE_MODELS.characterDesign.defaultSize`

### 引用图像
- **步骤 3 STYLE** 选定的风格图 — 作为 `referenceImageUrl` 传入图像 API（图生图）
  - 降级路径：若风格未执行 → 文生图

### 输出产物
- `outputData.portraits[]` — 每个角色含 characterId/name/assetId/url/llmPrompt

---

## 步骤 5: CONCEPT（概念图）

### 子任务清单

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 5-1 | 文本生成 | `TEXT_MODELS.conceptArt` | 步骤 2 FRAMEWORK + 步骤 3 STYLE 的风格提示词 | 6 个场景的英文图像提示词（3 幕 × 2 场景） |
| 5-2 | 图像生成 × 6 | `IMAGE_MODELS.conceptArt.primary` | 子任务 5-1 输出的 `imagePrompt` + 步骤 3 风格图 + 步骤 4 角色图 | 6 张概念场景图 |

### 提示词

**子任务 5-1 — 来源：文件 `prompts/concept.txt`**

```
角色：高端艺术电影 AI 摄影指导

目标：你是一家顶尖艺术电影工作室的创意总监。你的任务是根据客户的照片和整体分镜脚本，生成对应三幕的最有该幕代表性的风格指引性镜头的提示词，具有"奥斯卡级别"质感，每一幕各设计两个画面，明确地区分两个画面，两个画面内人物动作神态相互有所区别，要求这六张画面有着不同的景别和运镜逻辑，包括最近处的特写和最远处的大远景。

视觉风格：大师级商业摄影。35mm 胶片美学，模拟 Kodak Portra 400 胶卷，柔和梦幻的颗粒感。光线：匹配参考图的自然环境光，在适用时采用背光或镜头光晕，高动态范围。摄像：手持浮动斯坦尼康感，流畅且有机的运动。氛围：空灵、怀旧、亲密、神圣、诗意现实主义、情感共鸣、奥斯卡级别的调色，色调倾向明显且强景深。

系统指令 AI 规则集：
禁止摆拍 / 窥视视角 / 偏离中心 / 禁止剪影 / 电影感 / 不要描述主体的具体长相或穿着

输出格式（严格 JSON，不要 Markdown 代码块）：
{
  "scenes": [
    {"actNumber": 1, "sceneNumber": 1, "shotType": "特写/近景/中景/全景/大远景", "cameraMove": "推/拉/摇/移/跟/升/降/固定", "imagePrompt": "英文提示词..."}
  ]
}

整体灵感内容如下：
{{USER_INPUT}} = JSON.stringify({ framework, selectedStyle: stylePrompt })
```

**子任务 5-2 — 提示词 = 子任务 5-1 输出的 `imagePrompt`（优先）或 framework 原始场景描述（兜底）**

```
finalPrompt = llmScene.imagePrompt || act.scenes[i]
```

图像 API 调用时传入：
- `prompt` = `finalPrompt`
- `referenceImageUrl` = 步骤 3 风格图 URL
- `imageUrls[]` = 步骤 4 所有角色图 URL 数组（`doubao-multi` 模式下）

### 调用参数
- 文本：`temperature: 0.7`, `maxTokens: 4096`
- 图像：`IMAGE_MODELS.conceptArt.defaultSize`

### 引用图像（多图参考）
- **步骤 3 STYLE** 选定风格图 — `referenceImageUrl`
- **步骤 4 CHARACTER** 所有角色图 — `characterImageUrls[]`（`doubao-multi` 模式下 `image: [styleRefUrl, ...charUrls]`）

### 输出产物
- `outputData.scenes[]` — 每张含 actNumber/sceneIndex/url/prompt/llmPrompt/isMock

---

## 步骤 6: TRAILER（宣传片生成）

### 子任务清单

| 子任务 | 类型 | 模型/组件 | 输入 | 输出 |
|-------|------|----------|------|------|
| 6-1 | 文本生成 × 6（并行） | `TEXT_MODELS.TRAILER_PROMPT`（primary + fallback） | 步骤 2 FRAMEWORK（synopsis + acts mood） + 步骤 5 CONCEPT（llmPrompt/sceneDesc） | 6 个视频提示词 `videoPrompt` + 镜头运动描述 |
| 6-2 | 图生视频 × 6（串行） | Veo 3.1 Lite → 即梦 → 海螺 → Ken Burns Mock（四级降级） | 子任务 6-1 的 `videoPrompt` + 步骤 5 CONCEPT 的 6 张概念图 | 6 段 5s MP4 视频片段 |
| 6-3 | 视频裁剪 × N | ffmpeg trimVideo | 子任务 6-2 的每段视频 | 精确 5s 裁剪后的视频 |
| 6-4 | 视频上传 × 6 | R2 upload | 子任务 6-3 的每段视频 | 6 段可播放签名 URL |
| 6-5 | ffmpeg 拼接 | concat demuxer | 子任务 6-3 的 6 段 5s 视频 | 30s 无音轨 concat.mp4 |
| 6-6 | 音乐生成 | 千问百聆 → MiniMax → 30s 静音 AAC（三级降级） | 步骤 2 FRAMEWORK（幕的 mood） + synopsis（截取前 200/300 字符） | bgm.aac/mp3/wav |
| 6-7 | ffmpeg 裁剪音频 | trimAudio | 子任务 6-6 的 BGM | 30s 裁剪后的 bgm_trimmed_30s.m4a |
| 6-8 | BGM 上传 | R2 upload | 子任务 6-7 的 BGM | BGM 签名 URL |
| 6-9 | ffmpeg 混音 | mixAudioVideo | 子任务 6-5 的 concat.mp4 + 子任务 6-7 的 bgm_trimmed_30s.m4a | 30s 最终 final.mp4 |
| 6-10 | 最终上传 | R2 upload | 子任务 6-9 的 final.mp4 | trailer.mp4 签名 URL |

### 提示词

**子任务 6-1 — 动态构造（无文件模板），在 `lib/api-clients/mock-video.ts` 的 `generateVideoPromptForConcept()` 中构造：**

```
基于以下影视项目信息，为第{actNum}幕第{sceneNum}个场景生成一段视频生成提示词。
该视频将作为30秒宣传片的第{index+1}个5秒片段。

故事梗概：{storyBrief.slice(0, 400)}
场景画面描述：{sceneDesc.slice(0, 500)}
场景氛围：{mood}

要求：
- 提示词必须用英文
- 必须描述镜头运动（如 slow push-in / gentle pan / static with subtle light change / dolly forward）
- 必须描述画面动态（如 character breathing / smoke drifting / water rippling / hair moving in wind）
- 必须保持与概念图一致的色调和光影
- 严格输出 JSON（不要任何额外解释）：{"videoPrompt": "英文提示词", "cameraMotion": "镜头运动描述", "duration": 5}
```

其中：
- `storyBrief` = 来自 **步骤 2 FRAMEWORK** 的 `synopsis`
- `sceneDesc` = 来自 **步骤 5 CONCEPT** 的概念图 `llmPrompt` 或 `sceneDesc`
- `mood` = 来自 **步骤 2 FRAMEWORK** 对应幕的 `mood/tone` 字段

**子任务 6-6（音乐）— 千问百聆提示词，动态构造：**

```
史诗级电影预告片背景音乐，情绪: {overallMood}，风格: 管弦乐,紧张激烈,气势磅礴，故事背景: {storyBrief.slice(0, 200)}
```

**子任务 6-6（音乐）— MiniMax 提示词，动态构造（fallback）：**

```
Epic cinematic trailer music. Mood: {overallMood}. Style: orchestral, dramatic, intense, building tension. Story context: {storyBrief.slice(0, 300)}
```

### 引用图像
- **步骤 5 CONCEPT** 的 6 张概念图 — 每张作为图生视频的首帧 `imageUrl`

### 合成流程（完整产物链）

```
步骤 5 CONCEPT（6 张概念图）
    │
    ▼
[6-1] 文本生成 × 6 ──► 6 个 videoPrompt + cameraMotion
    │
    ▼
[6-2] 图生视频 × 6 ──► 6 段 ~5s MP4（seg_00.mp4 ~ seg_05.mp4）
    │                      ↑ Veo/即梦/海螺 真实 API，失败回退 Ken Burns
    ▼
[6-3] 视频裁剪 ──► 精确 5s 裁剪（seg_*_trim5s.mp4）
    │
    ▼
[6-4] 上传 R2 ──► 6 段签名 URL（用于前端预览）
    │
    ▼
[6-5] ffmpeg concat ──► concat.mp4（30s 无音轨视频）
         命令：-f concat -safe 0 -i list.txt -fflags +genpts -c:v libx264 ...
    │
    ▼
[6-6] 音乐生成 ──► bgm.aac/mp3/wav（30s BGM）
         千问百聆 → MiniMax → 30s 静音 AAC
    │
    ▼
[6-7] ffmpeg 裁剪音频 ──► bgm_trimmed_30s.m4a
    │
    ▼
[6-8] BGM 上传 R2 ──► BGM 签名 URL
    │
    ▼
[6-9] ffmpeg mixAudioVideo ──► final.mp4
         命令：-i concat.mp4 -i bgm_trimmed_30s.m4a -c:v copy -c:a aac -map 0:v:0 -map 1:a:0 -shortest
    │
    ▼
[6-10] 上传 R2 ──► projects/{id}/trailer.mp4
```

### 输出产物
- `outputData.videoUrl` — 最终 30s 预告片 URL
- `outputData.segments[]` — 6 段视频详情（videoUrl/prompt/cameraMotion/isMock/durationSec）
- `outputData.musicUrl` — BGM URL
- `outputData.musicIsMock` — BGM 是否为占位静音

---

## 步骤 7: STORYBOARD（分镜设计）

### 子任务清单

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 7-1 | 文本生成 × 3（并行） | `TEXT_MODELS.storyboard` | 步骤 2 FRAMEWORK 输出的完整 JSON | 30 个分镜镜头（3 幕各 10 个） |
| 7-2 | SVG 渲染 × 30（非 AI） | `sharp` 库 | 子任务 7-1 输出的每个 shot（shotId/cameraMove/actNumber/duration/description/characters） | 30 张分镜占位图 PNG |

### 提示词

**子任务 7-1 — 来源：3 个文件（三幕分别）**

**`prompts/storyboard-act1.txt`**（第一幕，shot_001 ~ shot_010）

```
角色：高端艺术电影 AI 分镜师

目标：你是一家顶级艺术电影工作室的分镜师。你的任务是根据影片框架（背景、角色、故事梗概），生成第一幕的分镜表。

要求：
1. 第一幕镜头数量：10 个（shot_001 到 shot_010）
2. 每个镜头必须有不同的景别和运镜逻辑，避免重复。
3. 角色出场必须与故事梗概一致，禁止凭空新增角色。
4. 每个镜头的描述必须包含：视角（POV/主观/客观）、画面内容、具体情节。
5. 第一幕是开篇，用于建立世界观和角色，节奏相对平稳。
6. 输出必须是严格的 JSON 数组格式，不要包含 Markdown 代码块标记。

JSON 字段规范：
- shotId: "shot_001" ~ "shot_010"
- actNumber: 1
- sceneName: 该幕场景名称
- description: 画面描述（含视角、画面内容、情节）
- cameraMove: 推镜头/拉镜头/摇镜头/移镜头/跟镜头/升镜头/降镜头/固定镜头/手持镜头/轨道横移
- duration: 3/5/7/10
- characters: 出场角色 ID 列表
- keyAction: 核心动作或情绪转折

影片框架信息如下：
{{USER_INPUT}} = JSON.stringify(framework)
```

**`prompts/storyboard-act2.txt`**（第二幕，shot_011 ~ shot_020）
- 区别：第二幕是冲突发展阶段，节奏逐步加快，运镜更加动态；shotId 从 shot_011 到 shot_020；actNumber = 2

**`prompts/storyboard-act3.txt`**（第三幕，shot_021 ~ shot_030）
- 区别：第三幕是高潮和结局，节奏紧凑，运镜充满张力然后逐渐舒缓；shotId 从 shot_021 到 shot_030；actNumber = 3

**子任务 7-2 — SVG 模板（代码内嵌，非文件模板）：**

```svg
<svg width="1024" height="576" xmlns="http://www.w3.org/2000/svg">
  <rect width="100%" height="100%" fill="#f8f9fa"/>
  <text x="50%" y="10%" font-family="system-ui" font-size="24" fill="#333" text-anchor="middle">分镜 {shotId}</text>
  <text x="50%" y="20%" font-family="system-ui" font-size="16" fill="#666" text-anchor="middle">{cameraMove} | 第{actNumber}幕 | {duration}秒</text>
  {彩色圆圈占位角色}
  <rect x="50" y="450" width="924" height="80" fill="none" stroke="#333" stroke-width="2" stroke-dasharray="8,4"/>
  <text x="50%" y="490" font-family="system-ui" font-size="14" fill="#333" text-anchor="middle">{description}</text>
</svg>
```

### 调用参数
- 文本：`temperature: 0.7`, `maxTokens: 4096`
- SVG：sharp 渲染为 PNG（`sharp(Buffer.from(svg)).png().toBuffer()`）

### 引用图像
无（角色占位圆圈仅为颜色占位，不引用真实角色图）

### 模式选择
用户在执行分镜设计时选择模式，影响后续产出：
- **实拍参考模式**（`mode: "reference"`）：产出 `referenceImageUrl`
- **视频生成模式**（`mode: "keyframe"`）：产出 `firstFrameUrl`

### 输出产物
- `outputData.shots[]` — 30 个分镜镜头
- `outputData.shotAssets[]` — 30 张分镜占位图
- `outputData.mode` — "reference" | "keyframe"

---

## 步骤 8: KEYFRAMES（生成尾帧）

### 子任务清单（两条路径）

#### 路径 A：批量生成（`POST /api/projects/[id]/steps/keyframes`）

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 8A-1 | 文本生成 × N | `TEXT_MODELS.keyframes` | 步骤 7 STORYBOARD 每个 shot 的 `description` + 步骤 3 的风格提示词 | N 个尾帧英文图像提示词 |
| 8A-2 | 图像生成 × N | `IMAGE_MODELS.keyframes.primary` | 子任务 8A-1 输出的 `imagePrompt` + 步骤 3 风格图 | N 张尾帧图像 |

#### 路径 B：单条生成（`POST /api/projects/[id]/steps/keyframes/generate-last`）

| 子任务 | 类型 | 模型 | 输入 | 输出 |
|-------|------|------|------|------|
| 8B-1 | 文本生成 × 1 | `TEXT_MODELS.keyframes` | 指定 shot 的 `description` + 步骤 3 的风格提示词 | 1 个尾帧英文图像提示词 |
| 8B-2 | 图像生成 × 1 | `IMAGE_MODELS.keyframes.primary` | 子任务 8B-1 输出的 `imagePrompt` + 步骤 3 风格图 | 1 张尾帧图像 |

### 提示词

**子任务 8A-1 / 8B-1 — 来源：文件 `prompts/keyframe-last.txt`**

```
角色：高端艺术电影 AI 关键帧画师

目标：根据分镜描述和首帧信息，生成该镜头结束瞬间的英文图像提示词。这张尾帧将作为视频生成的结束画面，必须与首帧保持角色和场景连续性。

全局风格与美学指令（适用于所有镜头）：
视觉风格：大师级商业摄影。35mm 胶片美学，模拟 Kodak Portra 400 胶卷，柔和梦幻的颗粒感。光线：自然环境光，背光或镜头光晕，高动态范围。摄像：手持浮动斯坦尼康感，流畅有机的运动。氛围：空灵、怀旧、亲密、神圣、诗意现实主义、情感共鸣、奥斯卡级调色，色调倾向明显且强景深。

系统指令 AI 规则集：
禁止摆拍 / 窥视视角 / 偏离中心 / 禁止剪影 / 电影感

要求：
1. 提示词必须包含：场景环境、角色姿态（动作结束状态）、光影、35mm胶片关键词、色调关键词、8K。
2. 这是镜头的"结束帧"，角色应处于动作完成后的状态，与首帧形成动作闭环。
3. 角色服装、外貌、场景必须与首帧保持一致，仅允许动作和表情的合理变化。
4. 必须严格遵循已选定的影片风格：{{STYLE_REF}}

输出格式（严格 JSON）：
{
  "keyframe": {
    "shotId": "shot_001",
    "frameType": "last",
    "imagePrompt": "英文提示词..."
  }
}

分镜描述如下：
{{USER_INPUT}} = shot.description || "{shotId} - {sceneName}"
```

**子任务 8A-2 / 8B-2 — 提示词 = 子任务 8A-1/8B-1 输出的 `imagePrompt`**

图像 API 调用时：
- `prompt` = LLM 生成的 `imagePrompt`
- `referenceImageUrl` = 步骤 3 风格图 URL

### 引用图像
- **步骤 3 STYLE** 选定风格图 — 作为 `referenceImageUrl` 传入图像 API（图生图）
- **步骤 7 STORYBOARD** 首帧（`firstFrameUrl` 或 `referenceImageUrl`）— 只读显示，**不作为生成参考图传入**

### 输出产物
- `outputData.results[]` / `outputData.keyframes[]` — 每个含 shotId/firstFrameUrl/lastFrameUrl/description
- 同时，`lastFrameUrl` 写回 `STORYBOARD.outputData.shots[i].lastFrameUrl`（单一数据源）

---

## 步骤 9: VIDEO_DIRECT（直生视频）

### 子任务清单

| 子任务 | 类型 | 组件 | 输入 | 输出 |
|-------|------|------|------|------|
| 9-1 | 任务入队 | BullMQ 队列 | 步骤 7 的首帧 + 步骤 8 的尾帧（可选） | N 个队列任务 |
| 9-2 | 首帧下载 | R2 download → 本地临时文件 | 步骤 7 STORYBOARD `firstFrameUrl`/`referenceImageUrl` | first.png |
| 9-3 | 尾帧下载（可选） | R2 download → 本地临时文件 | 步骤 8 KEYFRAMES `lastFrameUrl` | last.png |
| 9-4 | ffmpeg xfade 合成 | ffmpeg complexFilter | first.png + last.png（或仅 first.png） | shot-{shotId}.mp4（2s） |
| 9-5 | 上传 R2 | R2 upload | 子任务 9-4 的 MP4 | 可播放签名 URL |

### 提示词
无（本步骤不调用 AI 生成模型，纯 ffmpeg 合成）

### 合成流程

**策略 A：first-last（有尾帧）**

```
步骤 7 STORYBOARD firstFrameUrl（首帧 PNG）
       +
步骤 8 KEYFRAMES lastFrameUrl（尾帧 PNG）
       │
       ▼
ffmpeg:
  [0:v]scale=1024:576,setsar=1[first];
  [1:v]scale=1024:576,setsar=1[last];
  [first][last]xfade=transition=fade:duration=0.5:offset=0.5[outv]
       │
       ▼
2s MP4（libx264, yuv420p, 30fps, fade 过渡 0.5s）
       │
       ▼
上传 R2 → projects/{id}/videos/{shotId}.mp4
```

**策略 B：first-only（无尾帧，Ken Burns 效果）**

```
步骤 7 STORYBOARD firstFrameUrl（首帧 PNG）
       │
       ▼
ffmpeg zoompan（缓慢缩放 2s）
       │
       ▼
2s MP4
       │
       ▼
上传 R2 → projects/{id}/videos/{shotId}.mp4
```

### 引用图像
- `firstFrameUrl`：来自 **步骤 7 STORYBOARD**（分镜设计视频生成模式产生的起始帧）
- `lastFrameUrl`：来自 **步骤 8 KEYFRAMES**（尾帧，可选）

### 输出产物
- `outputData.clips[]` — 每个含 shotId/url/duration

---

## 提示词来源追踪表

| 步骤 | 子任务 | 提示词来源 | 备注 |
|------|-------|-----------|------|
| 1 IDEATION | 1-1 文本 | 文件 `prompts/ideation.txt` | `{{USER_INPUT}}` = project.rawIdea |
| 2 FRAMEWORK | 2-1 文本 | 文件 `prompts/ideation.txt` | `{{USER_INPUT}}` = rawIdea + 选定方向锚定 |
| 3 STYLE | 3-1 文本 | 文件 `prompts/style-generation.txt` | 注入 synopsis/visualStyle/mood |
| 3 STYLE | 3-2 图像 × 3 | 子任务 3-1 输出的 `prompt` 字段 | 无固定后缀，直接使用 |
| 4 CHARACTER | 4-1 文本 | 文件 `prompts/character.txt` | `{{USER_INPUT}}` = JSON.stringify(framework) |
| 4 CHARACTER | 4-2 图像 × N | 子任务 4-1 的 `imagePrompt` + framework.description | 风格通过 referenceImageUrl 注入 |
| 5 CONCEPT | 5-1 文本 | 文件 `prompts/concept.txt` | `{{USER_INPUT}}` = JSON.stringify({framework, selectedStyle}) |
| 5 CONCEPT | 5-2 图像 × 6 | 子任务 5-1 的 `imagePrompt` 或 framework.scenes | 多图参考：风格图 + 角色图 |
| 6 TRAILER | 6-1 文本 × 6 | `mock-video.ts` 动态构造 | 输入=FRAMEWORK synopsis + CONCEPT sceneDesc + FRAMEWORK mood |
| 6 TRAILER | 6-2 视频 × 6 | 子任务 6-1 的 `videoPrompt` | 图生视频，首帧=CONCEPT 概念图 |
| 6 TRAILER | 6-6 音乐 | `mock-video.ts` 动态构造 | 千问百聆(中文) / MiniMax(英文) |
| 7 STORYBOARD | 7-1 文本 × 3 | 文件 `prompts/storyboard-act1/2/3.txt` | `{{USER_INPUT}}` = JSON.stringify(framework) |
| 7 STORYBOARD | 7-2 SVG × 30 | 代码内嵌 SVG 模板 | sharp 渲染，非 AI 生成 |
| 8 KEYFRAMES | 8A-1/8B-1 文本 | 文件 `prompts/keyframe-last.txt` | `{{USER_INPUT}}` = shot.description, `{{STYLE_REF}}` = 步骤3风格 |
| 8 KEYFRAMES | 8A-2/8B-2 图像 | 子任务 8A-1/8B-1 的 `imagePrompt` | 风格通过 referenceImageUrl 注入 |
| 9 VIDEO_DIRECT | 无 | 无 AI 调用 | 纯 ffmpeg 合成 |

---

## 参考图依赖追踪表

| 步骤 | 子任务 | 是否使用参考图 | 参考图来源 | 引用方式 |
|------|-------|-------------|-----------|---------|
| 1 IDEATION | 全部 | 否 | - | - |
| 2 FRAMEWORK | 全部 | 否 | - | - |
| 3 STYLE | 3-1 文本 | 否 | - | - |
| 3 STYLE | 3-2 图像 × 3 | 否 | - | 纯文生图 |
| 4 CHARACTER | 4-1 文本 | 否 | - | - |
| 4 CHARACTER | 4-2 图像 × N | 是 | **步骤 3 STYLE** 选定的风格图 | `referenceImageUrl` 图生图 |
| 5 CONCEPT | 5-1 文本 | 否 | - | - |
| 5 CONCEPT | 5-2 图像 × 6 | 是 | **步骤 3 STYLE** 风格图 + **步骤 4 CHARACTER** 所有角色图 | `doubao-multi` 多图参考 `[styleRefUrl, ...charUrls]` |
| 6 TRAILER | 6-2 视频 × 6 | 是 | **步骤 5 CONCEPT** 的 6 张概念图 | 图生视频首帧 `imageUrl` |
| 7 STORYBOARD | 全部 | 否 | - | SVG 占位图 |
| 8 KEYFRAMES | 8A-1/8B-1 文本 | 否 | - | 风格提示词注入 `{{STYLE_REF}}` |
| 8 KEYFRAMES | 8A-2/8B-2 图像 | 是 | **步骤 3 STYLE** 风格图 | `referenceImageUrl` 图生图 |
| 9 VIDEO_DIRECT | 全部 | 是 | **步骤 7 STORYBOARD** 首帧 + **步骤 8 KEYFRAMES** 尾帧 | ffmpeg xfade 输入 |

---

## 合成产物链完整拆解

### TRAILER 完整合成链

```
步骤 2 FRAMEWORK synopsis/mood
       +
步骤 5 CONCEPT sceneDesc/llmPrompt
       │
       ▼ [子任务 6-1: 文本生成 × 6]
6 个 videoPrompt + cameraMotion
       +
步骤 5 CONCEPT 6 张概念图（作为首帧）
       │
       ▼ [子任务 6-2: 图生视频 × 6]
6 段 ~5s MP4（seg_00 ~ seg_05）
       │
       ▼ [子任务 6-3: ffmpeg trimVideo × 6]
6 段精确 5s MP4
       │
       ▼ [子任务 6-5: ffmpeg concat]
concat.mp4（30s 无音轨）
       +
步骤 2 FRAMEWORK mood + synopsis
       │
       ▼ [子任务 6-6: 音乐生成]
bgm.aac/mp3/wav
       │
       ▼ [子任务 6-7: ffmpeg trimAudio]
bgm_trimmed_30s.m4a
       │
       ▼ [子任务 6-9: ffmpeg mixAudioVideo]
final.mp4 = concat.mp4 + bgm_trimmed_30s.m4a
       │
       ▼ [子任务 6-10: 上传 R2]
projects/{id}/trailer.mp4
```

### VIDEO_DIRECT 完整合成链

```
步骤 7 STORYBOARD firstFrameUrl
       │
       ▼ [子任务 9-2: 下载到本地]
first.png
       +（可选）
步骤 8 KEYFRAMES lastFrameUrl
       │
       ▼ [子任务 9-3: 下载到本地]
last.png
       │
       ▼ [子任务 9-4: ffmpeg xfade 或 zoompan]
shot-{shotId}.mp4（2s）
       │
       ▼ [子任务 9-5: 上传 R2]
projects/{id}/videos/{shotId}.mp4
```

---

## 未使用的 Prompt 模板

以下 5 个模板文件存在但 **当前无任何 API 路由调用**：

| 模板文件 | 预期用途 | 状态 |
|---------|---------|------|
| `prompts/review-consistency.txt` | 相邻镜头一致性质检（角色/服装/光影/场景） | 未使用 |
| `prompts/review-emotion.txt` | 情感曲线分析（剧本医生，检测逻辑漏洞） | 未使用 |
| `prompts/style.txt` | 风格锚点提示词生成（3 种影调方向，同一场景/构图） | 未使用，实际使用 `style-generation.txt` |
| `prompts/storyboard.txt` | 一次性 30 镜头分镜表（不分幕） | 未使用，实际使用按幕拆分的 3 个模板 |
| `prompts/keyframe-first.txt` | 首帧提示词生成 | 未使用，当前只生成尾帧 |

---

## 关键发现

1. **风格图是整个图像管线的唯一参考源**：`lib/style-ref.ts` 的 `getStyleRefUrl()` 被 CHARACTER、CONCEPT、KEYFRAMES 三个步骤统一调用，确保所有图像生成使用同一张选定风格图。

2. **CONCEPT 步骤使用多图参考**：同时传入风格图 + 所有角色图给 `doubao-multi` 模型（`seedream-4-0`），在单次生图调用中实现风格和角色的双约束。

3. **TRAILER 步骤是最复杂的合成管线**：包含文本提示词生成 → 图生视频 → 视频裁剪 → 视频上传 → 视频拼接 → 音乐生成 → 音频裁剪 → 音画混音 → 最终上传，共 5 类 AI 生成任务和 5 次 ffmpeg 操作。

4. **VIDEO_DIRECT 步骤是纯 ffmpeg 合成**：不调用任何 AI 模型，仅将步骤 7 的首帧和步骤 8 的尾帧通过 xfade 滤镜合成为 2s 视频。

5. **5 个 Prompt 模板未被使用**：`review-consistency.txt`, `review-emotion.txt`, `style.txt`, `storyboard.txt`, `keyframe-first.txt` 均无对应的 API 路由调用，可能是为未来功能预留。

6. **视频生成全部走异步**：TRAILER 通过 `setImmediate` 后台处理或 BullMQ 队列异步执行；VIDEO_DIRECT 通过 BullMQ 队列异步执行。

7. **提示词来源分两类**：
   - **文件模板**（9 个）：`ideation.txt`（步骤 1+2）、`style-generation.txt`（步骤 3）、`character.txt`（步骤 4）、`concept.txt`（步骤 5）、`storyboard-act1/2/3.txt`（步骤 7）、`keyframe-last.txt`（步骤 8）
   - **运行时动态构造**（3 个）：TRAILER 视频提示词（`mock-video.ts`）、TRAILER 音乐提示词（`mock-video.ts`）、STORYBOARD SVG 模板（代码内嵌）
